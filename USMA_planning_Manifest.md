# USMA — Unified Solution Migration Analyzer — Planning Manifest

> Living progress tracker for the multi-source refactor described in [feasibility_study.md](feasibility_study.md).
>
> **Branch:** `unified_SMA`
> **Working product name:** Unified Solution Migration Analyzer (USMA). Final brand TBD via ADR-0004.
> **How to update:** check off boxes as items land; bump phase status when exit criteria are met.

---

## Phase status overview

| Phase | Title | Status | Notes |
|---|---|---|---|
| 0 | Decisions + scaffolding | **🟢 decisions locked** | Scaffolding skeleton landed; D1–D10 all signed off 2026-05-19. PR merge to `main` is the last open item. |
| 1 | Foundational refactor (no new source) | **🟢 backend complete** | Provider + scopes + plan_run + manifest v2 shipped. Phase-1.5 carve-out (CLI `--scope`, SourcePicker wiring, MODULE_SPECS callers) tracked separately. |
| 2 | ADF as second source | **🟢 end-to-end** | Single-source ADF SPA path is fully green: Configuration radio + factory discovery + REPLACE UX + run-page module gating + ADF run-history client (counts / success / daily+hourly status). Multi-scope plumbing now reaches the SPA: backend per-scope dispatch (2.7-A), scope-aware artefact serving + `/scopes` route (2.7-B), and SPA `loadModulePerScope()` (2.7-C). Phase 3 UI can fan out across scopes without further plumbing. |
| 2.7 | Multi-scope analyzer dispatch | **� complete** | All three slices shipped: 2.7-A `JobRunner._run` per-scope loop + per-scope subdirs (`2bceef6`); 2.7-B scope-aware artefact serving + `/scopes` route; 2.7-C SPA `loadModulePerScope()`. Phase 3 UI fans out across scopes without further plumbing. |
| 3 | Unified UI polish | **🟢 complete** | All Phase-3 UI slices landed including source-platform alignment (A–D, 2026-05-20). Deferred package directory rename to `usma/` with `DeprecationWarning` shim shipped per ADR-0004 (working tree is now `src/usma/`; brand rename to "Unified Solution Migration Analyzer" landed in `fe01d14`, 5.x.x). |
| 4 | Databricks | **🟢 Complete (alpha)** | Slices 4-A through 4-F-2 shipped (commits `4e65dbc`, `5cff105`, `bba5cbc`, `3a22c3b`, `d4d67b3`, `5d594bd`, `3cb6cf3`→`ebc3737`, `7a26cd8`, `bc190b5`; 524 total tests). Backend provider + `databricks_workflows` module + cost/fabric_mapping widening + SPA Configuration / Run page + user-guide docs + run-history vCore-hour collection + Dashboard tiles & charts + Estate Overview CU rollup all landed. DLT / Unity Catalog deferred to Phase 4.6; **Databricks on AWS** scoped as Phase 4.7. |
| 4.7 | Databricks on AWS (multi-cloud) | ⚪ not started | Scoped 2026-05-27 (Option A — sibling provider + `extras["platform"]` discriminator; reuses the entire `databricks_workflows` module unchanged). 6 slices (4.7-A → 4.7-F); ~2–4 dev-days for alpha. Cost on AWS deferred to Phase 4.7.5. |
| 5 | Google BigQuery | **🟢 Complete (alpha)** | Replaces previously-scoped SAP BW (deferred to Phase 6+). Slices 5-A through 5-J shipped: provider + `bigquery_workloads` module + slot→CU rollup + fabric_mapping + SPA Configuration + docs + estate-overview CU rollup + GCP cost client + browser ADC sign-in + Dashboard BigQuery section (headline tiles, side-by-side daily/hourly bar charts, GCS storage view, top-tables, breakdowns, sqlglot query-features). BigLake / Omni / BigQuery ML deep dive deferred. |
| 7 | Snowflake | **🟢 Complete (alpha)** | Slices 7-A through 7-J shipped (`c75bf34` module, `8961bc5` credit→vCore→CU rollup, `6e5199d`/`3a04b2d`/`02cf073`/`81bba2a` SPA Config + browser OAuth, `fc41c28` estate rollup + docs + cost client, `5f80048` dashboard section, `459a6d5` 7-day daily + hourly bars), plus production hardening (`50d1723` IMPORTED-PRIVILEGES role fallback, `4fdc2a7` credits-only degradation, `b2e3b32`/`be6182d`/`b9bc900`/`8ff16d2` cost-finding wording + role diagnostics, `2ab35b9` load_config branch). Snowflake-on-AWS primary; Azure/GCP variants reuse the provider via `extras["platform"]`. ADR-0007 (auth model) and a Snowpark-driven scope inspection are the deferred bits. |
| 5.3 | Cross-platform / packaging | **🟢 complete (5.3.x)** | OS-aware platform helpers in [src/usma/platform.py](src/usma/platform.py) (XDG on Linux/macOS, Known Folders on Windows), `default_odbc_driver()` with auto-fallback from driver 18 → 17, `odbc_install_hint()` per OS. [quickstart.sh](quickstart.sh) mirrors `quickstart.ps1` for Linux/macOS. QUICKSTART.md ships apt/brew install snippets for `msodbcsql18` and a Linux/macOS fast-path section. 14 new tests in [tests/test_platform.py](tests/test_platform.py). Shipped as 5.3.0 (`1859812`, `ed9522b`). |
| 6+ | Subsequent sources | ⚪ not started | |

Legend: ⚪ not started · 🟡 in progress · 🟢 done · 🔴 blocked

---

## Phase 0 — Decisions + scaffolding

Goal: Land design doc, ADR stubs, source/module scaffolding, and contributor docs. **Zero behavior change.**

### Documents
- [x] [feasibility_study.md](feasibility_study.md) — feasibility study landed on `unified_SMA`
- [x] [USMA_planning_Manifest.md](USMA_planning_Manifest.md) — this file
- [x] [docs/adr/README.md](docs/adr/README.md) — ADR index
- [x] [docs/adr/0001-multi-source-architecture.md](docs/adr/0001-multi-source-architecture.md) — stub
- [x] [docs/adr/0002-scope-and-credentials-model.md](docs/adr/0002-scope-and-credentials-model.md) — stub
- [x] [docs/adr/0003-manifest-v2-schema.md](docs/adr/0003-manifest-v2-schema.md) — stub
- [x] [docs/adr/0004-package-rename.md](docs/adr/0004-package-rename.md) — stub
- [x] [docs/architecture/overview.md](docs/architecture/overview.md) — stub
- [x] [docs/architecture/sources.md](docs/architecture/sources.md) — stub
- [x] [docs/architecture/modules.md](docs/architecture/modules.md) — stub
- [x] [docs/user-guide/99-adding-a-source.md](docs/user-guide/99-adding-a-source.md) — contributor guide stub

### Python `sources/` package (Phase-0 stub — no wire-up yet)
- [x] [src/synapse_migration_analyzer/sources/__init__.py](src/synapse_migration_analyzer/sources/__init__.py) — `SourceType`, `SourceDescriptor`, `Credentials`, `SourceProvider`, `SOURCE_REGISTRY`
- [x] [src/synapse_migration_analyzer/sources/base.py](src/synapse_migration_analyzer/sources/base.py) — `BaseSourceProvider`, `ConfigCheck`, `register_provider`
- [x] [src/synapse_migration_analyzer/sources/README.md](src/synapse_migration_analyzer/sources/README.md) — contributor doc
- [x] [src/synapse_migration_analyzer/sources/synapse_workspace/__init__.py](src/synapse_migration_analyzer/sources/synapse_workspace/__init__.py)
- [x] [src/synapse_migration_analyzer/sources/synapse_workspace/provider.py](src/synapse_migration_analyzer/sources/synapse_workspace/provider.py) — `NotImplementedError` stub; real impl in Phase 1
- [x] [src/synapse_migration_analyzer/sources/adf/__init__.py](src/synapse_migration_analyzer/sources/adf/__init__.py)
- [x] [src/synapse_migration_analyzer/sources/adf/provider.py](src/synapse_migration_analyzer/sources/adf/provider.py) — stub for Phase 2
- [x] [src/synapse_migration_analyzer/sources/databricks/__init__.py](src/synapse_migration_analyzer/sources/databricks/__init__.py)
- [x] [src/synapse_migration_analyzer/sources/databricks/provider.py](src/synapse_migration_analyzer/sources/databricks/provider.py) — stub for Phase 4
- [x] [src/synapse_migration_analyzer/sources/sap_bw/__init__.py](src/synapse_migration_analyzer/sources/sap_bw/__init__.py)
- [x] [src/synapse_migration_analyzer/sources/sap_bw/provider.py](src/synapse_migration_analyzer/sources/sap_bw/provider.py) — stub for Phase 5

### Python `modules/` ModuleSpec (additive — does not replace MODULE_REGISTRY yet)
- [x] [src/synapse_migration_analyzer/modules/spec.py](src/synapse_migration_analyzer/modules/spec.py) — `ModuleSpec` dataclass + `MODULE_SPECS` dict

### Tests
- [x] [tests/sources/__init__.py](tests/sources/__init__.py)
- [x] [tests/sources/test_registry.py](tests/sources/test_registry.py) — registry lookup
- [x] [tests/sources/test_source_descriptor.py](tests/sources/test_source_descriptor.py) — descriptor construction
- [x] [tests/sources/test_module_spec.py](tests/sources/test_module_spec.py) — `supports` filtering

### Frontend stubs (not yet wired into pages)
- [x] [web/src/components/SourcePicker.tsx](web/src/components/SourcePicker.tsx) — generic source-type tabbed picker
- [x] [web/src/components/ScopeList.tsx](web/src/components/ScopeList.tsx) — selected-scopes table
- [x] [web/src/components/EstateSummaryCard.tsx](web/src/components/EstateSummaryCard.tsx) — Phase-3 estate card

### Decisions to lock (see §9 of feasibility study)
- [x] D1 — **multi-scope per run** ✅ *(signed off 2026-05-19; manifest, `plan_run()`, and `ScopeRecord` already built around this in Phase 1; CLI/SPA surface still single-scope and exposes multi-scope in Phase 1.5+)*
- [x] D2 — **rename = yes, Phase 3, name = USMA** (Unified Solution Migration Analyzer) ✅ *(signed off 2026-05-19; one-release deprecation shim re-exporting `synapse_migration_analyzer` with `DeprecationWarning`; CLI entry point keeps both `synapse-migration-analyzer` and new `usma` commands during shim window)*
- [x] D3 — **shared `ClientSecretCredential` + `Credentials.extras: dict[str, str]` for per-source secrets** ✅ *(signed off 2026-05-19; already implemented as `sources.Credentials`; per-scope override possible but not the default)*
- [x] D4 — **source-type tabs in Configuration** ✅ *(signed off 2026-05-19; single Synapse tab visible in Phase 1.5; additional tabs unlock as sources ship in Phases 2/4/5)*
- [x] D5 — **keep `pipelines` for now; rename to `orchestration` in Phase 4** ✅ *(signed off 2026-05-19; ADF Phase 2 also uses "pipeline" terminology so the rename isn't forced until Databricks/SAP BW arrive)*
- [x] D6 — **optional native deps via extras** ✅ *(signed off 2026-05-19; `pip install usma[sapbw]`, `[databricks]`, `[ssis]`; keeps core install free of `pyrfc`/`pyodbc` native build prereqs)*
- [x] D7 — **manifest v2 source_type/scope_id per-`ArtifactRecord` + top-level `scopes[]`** ✅ *(signed off 2026-05-19; shipped in Phase 1 — see `reporting/run_manifest.py`)*
- [x] D8 — **`<source_type>__<short_scope_id>__<module>.json`** where `short_scope_id` is a readable lowercase fs-safe slug (NOT a hash) ✅ *(signed off 2026-05-19; correction vs original wording — hashes are unreadable in directory listings and break runbook grep)*
- [x] D9 — **`supports: frozenset[SourceType]` hardcoded on `ModuleSpec`** ✅ *(signed off 2026-05-19; YAML/DB-driven declarations deferred until external plugins exist, per ADR-0001)*
- [x] D10 — **keep current React pattern; introduce `ScopeContext` only when ≥3 consumers need scope without prop drilling** ✅ *(signed off 2026-05-19; Phase 1.5 lifts state to `Configuration.tsx`)*

### Phase 0 exit criteria
- [x] Scaffolding skeleton committed
- [x] Phase-0 tests pass (`pytest tests/sources/`)
- [x] Decisions D1–D10 signed off
- [ ] PR merged to `main`

---

## Phase 1 — Foundational refactor (no new source)

Goal: Make the existing Synapse code use the new abstractions. Zero user-visible regression.

### Backend
- [x] Move Synapse discovery from `web/config_io.py` into `sources/synapse_workspace/provider.py`
- [x] `web/config_io.py` becomes a thin shim over `SOURCE_REGISTRY[SYNAPSE_WORKSPACE]` (for `discover_workspaces`; `validate_config_live` still inline — converged in Phase 2)
- [x] `config.py` adds `scopes: tuple[SourceDescriptor, ...]` (legacy `SYNAPSE_WORKSPACE_NAME` + `SYNAPSE_RESOURCE_GROUP` auto-populates a single scope)
- [ ] `doctor.py` per-scope validation *(deferred → Phase 1.5)*
- [ ] `cli.py` accepts `--scope` (repeatable) on `analyze-all` *(deferred → Phase 1.5)*
- [ ] `web/api/runs.py` accepts scope context per request *(deferred → Phase 1.5)*
- [x] `reporting/run_manifest.py` bumped to schema v2 (`source_type`, `scope_id` per artifact; top-level `scopes[]`)
- [x] Manifest v1 → v2 auto-upgrade on read (read-only; never rewrites v1 files on disk)
- [ ] Replace `MODULE_REGISTRY` callers to consume `MODULE_SPECS` (with `supports` filter) *(deferred → Phase 1.5; mechanical, no behavioral change)*
- [x] Run plan helper (`plan_run(modules, scopes) -> RunPlan`)

### Frontend (all deferred → Phase 1.5; SPA behavior unchanged in Phase 1 per exit criteria)
- [ ] `web/src/api/loader.ts` — `apiDiscoverByType(sourceType)`, scopes in `RunRequest`
- [ ] `Configuration.tsx` — wire `SourcePicker` + `ScopeList`, hidden tabs (only Synapse visible) — visually identical to today
- [ ] `Run.tsx` — module × scope matrix (single scope still renders as today)

### Tests
- [x] `tests/test_run_plan.py` — module × scope filtering (9 tests)
- [x] `tests/test_manifest_v2.py` — schema bump + v1 upgrade (7 tests)
- [x] `tests/sources/synapse_workspace/test_provider.py` — provider tests (6 tests)
- [x] All targeted tests green (59 passing across sources/, run_plan, manifests, config)

### Docs
- [ ] `docs/user-guide/01-getting-started.md` — multi-source intro *(deferred → Phase 1.5)*
- [ ] `docs/user-guide/02-modes.md` — introduce "scope" concept *(deferred → Phase 1.5)*
- [x] `docs/architecture/sources.md` — Phase 1 content
- [x] `docs/architecture/modules.md` — Phase 1 content (plan_run, manifest v2)

### Phase 1 exit criteria
- [x] All existing tests green (manifest v1 tests still pass after schema bump)
- [x] CLI usage unchanged (no surface change to `analyze-all` / `doctor` / `serve`)
- [x] SPA behavior unchanged (Configuration page UX preserved by `discover_workspaces` shim)
- [x] Manifest schema v2 with auto-upgrade for v1

### Phase 1.5 — follow-up tickets (carved out to keep Phase 1 commit reviewable) ✅ closed 2026-05-20
- [x] `cli.py` `--scope` (repeatable) on `analyze-all` *(commit `3b27727` — `_apply_scope_flags` parser + 6 unit tests)*
- [x] `doctor.py` per-scope validation via `SourceProvider.validate` *(commit pending — new `_check_scopes()` registered in `run_checks`; per-scope `CheckResult` with provider-aggregated detail; gracefully skips when offline / AAD env missing / `load_config` fails; 5 new tests in `test_doctor.py`)*
- [x] `web/api/runs.py` optional `scopes: list[ScopeRef]` field (defaults to legacy single-scope) *(commit `3b27727` — `ScopeRef` Pydantic model + `StartRunRequest.scopes`, forwarded into `JobRunner.start`)*
- [x] ~~Replace MODULE_REGISTRY callers with MODULE_SPECS~~ *(partial: `run_plan.py` is on `MODULE_SPECS`; `modules/__init__.py` keeps `MODULE_REGISTRY` as the runtime dispatch table. Both coexist additively — full sweep deferred to package-rename PR per ADR-0004 since the import sites collapse there anyway)*
- [x] ~~Frontend: render single-tab `SourcePicker` + `ScopeList`~~ **superseded** *(Phase-0 stub components were never mounted. `Configuration.tsx` grew its own per-source radio + `scopeNoun() / scopeLabel() / discoverFor()` helpers in Slice 2.5 commit `7337c06` + Slice 4-D commit `3a22c3b`. Stubs deleted 2026-05-20; `SourceTypeId` lifted to `web/src/components/sourceTypes.ts`)*
- [x] ~~Frontend: `apiDiscoverByType(sourceType)` in `loader.ts`~~ **superseded** *(per-source typed functions shipped instead: `apiDiscoverWorkspaces` for Synapse, `apiDiscoverFactories` for ADF, `apiDiscoverDatabricksWorkspaces` for Databricks. Same behaviour, no generic dispatcher needed.)*
- [x] User-guide `01-getting-started.md` "scope" concept *(commit `d4d67b3` — Mermaid widened to source-type branch + scope fan-out + per-scope artefacts + scope filter pill node)*. `02-modes.md` left as-is — that chapter is scoped to static vs control-plane mode, not to the scope concept; adding it there would be off-topic.

---

## Phase 2 — ADF as second source

### Backend
- [x] Add `azure-mgmt-datafactory` to `pyproject.toml`
- [x] Implement `sources/adf/provider.py` (discover, validate, make_clients)
- [x] `modules/pipelines/sources/synapse_collector.py` — extract from current `artifacts_client.py` *(adapter shipped; full extraction lives in `artifacts_client.py` still — deferred to dispatch-wiring PR)*
- [x] `modules/pipelines/sources/adf_collector.py` — ADF data-plane via DataFactoryManagementClient *(structural; SDK calls + activity adapter wired, end-to-end fixture pass deferred)*
- [x] `modules/pipelines/analyzer.py` — dispatches by `source_type` (commit `<this>`: `collector=` kwarg + `for_descriptor()` factory; run-history step gated on Synapse collector)
- [x] `MODULE_SPECS['pipelines'].supports += {ADF}`
- [x] `MODULE_SPECS['cost'].supports += {ADF}`
- [x] `MODULE_SPECS['fabric_mapping'].supports += {ADF}`

### Frontend
- [x] `Configuration.tsx` — source-type radio (Synapse / ADF) with factory discovery + REPLACE UX *(commit `7337c06` + `b1beb84`; tabs deferred to Phase 4 when a third source lands)*
- [x] `Run.tsx` — module checkboxes filtered by source_type so only valid analyzers are shown / submitted *(commit `8867902`; ADF picker shows only `pipelines`)*

### Tests + fixtures
- [x] `tests/sources/adf/test_provider.py` (6 tests; mocked Azure SDK)
- [x] `tests/test_pipeline_collectors.py` (8 tests; protocol conformance + static helpers)
- [x] `tests/fixtures/sources/adf/` — sample ADF JSON payloads (`factory.json` covering pipelines, linked services, datasets, triggers, IRs)
- [x] Multi-scope run integration test (`tests/test_pipelines_analyzer_dispatch.py` — exercises injected ADF collector end-to-end without Azure; now also covers the ADF run-history dispatch)

### Docs
- [x] `docs/user-guide/20-data-factory.md` — new (ADF alpha overview, support matrix, `--scope` flag reference)
- [x] Update getting-started Mermaid *(commit `d4d67b3` — source-type branch covers Synapse / ADF / Databricks + per-scope fan-out)*. Modes diagram intentionally left single-flow — that chapter contrasts static vs control-plane mode, not source types.

### Phase 2 exit criteria
- [x] User can declare an ADF factory as a scope on the run (CLI `--scope adf:<name>` and SPA Configuration radio)
- [x] ADF inventory produces pipelines, triggers (with resolved cadence), and run history end-to-end
- [ ] Dashboard shows merged pipelines view *(needs multi-scope analyzer dispatch — Phase 2.7)*
- [x] Diff handles multi-scope correctly *(commit `172a16e` — scope-aware backend walker + Scope column on `RunDiff.tsx`)*

### Phase 2.5 / 2.6 — single-source ADF SPA path (landed this session)
- [x] `SMA_SOURCE_TYPE` env plumbed through `config.load_config` → `modules.__init__._pipelines` dispatch (commit `7337c06`)
- [x] `web/schemas.AzureConfigPublic/Update.source_type`; `web/config_io._SOURCE_TYPE_KEY` read/write
- [x] `POST /api/config/discover-factories` returning the same shape as `discover_workspaces`
- [x] `Configuration.tsx` source-type radio replaces the workspace dropdown based on the selected source
- [x] `validate_config_live` ADF branch with provider-dataclass → Pydantic-schema `ConfigCheck` conversion (commit `b1beb84` — fixes the validation 500)
- [x] `AdfCollector.endpoint` returns the factory ARM ID so `PipelinesAnalysis.artifacts_endpoint` is meaningful for ADF (commit `f0fa574`)
- [x] `Trigger.type_properties` carries the raw recurrence payload so `schedule_mapper` resolves cadence for ADF triggers (commit `f0fa574`)
- [x] `Run.tsx` `MODULES_BY_SOURCE` allow-list hides Synapse-only analyzers when `source_type === "adf"` (commit `8867902`)
- [x] `AdfRunHistoryClient` — drop-in mirror of `RunHistoryClient` over `DataFactoryManagementClient.pipeline_runs.query_by_factory` / `activity_runs.query_by_pipeline_run`; analyzer dispatches by `isinstance(collector, AdfCollector)` (commit `f45451f`)
- [x] 421 Python tests passing, 16 vitest tests passing, `tsc --noEmit` clean

### Phase 2.7 — multi-scope analyzer dispatch (✅ done)

Genuine end-to-end Phase 3 (merged Dashboard pipelines view, filter pills, multi-scope Diff) requires the runner to actually execute modules across multiple scopes per run. Landing in three slices so each push stays small and reviewable:

- [x] **2.7-A — backend dispatch** (commit `2bceef6`): `JobRunner._run` per-scope inner loop; `_scope_slug` / `_cfg_for_scope` / `_stamp_source_type_env` / `_is_multi_scope` helpers; per-scope artefacts land in `run_dir/<source_type>__<slug>/`. Single-scope runs keep the legacy flat layout (zero regression for the 421-test baseline). 7 new tests; **428 Python tests passing**.
- [x] **2.7-B — backend scope-aware artefact serving** (commit `762e3ca`): `GET /api/runs/{id}/scopes` enumerates per-scope subdirs; `GET /api/runs/{id}/modules/{module}` accepts an optional `?scope=<source_type>__<slug>` query parameter; `FilesystemRunRepo.scope_module_path` + `list_scopes` enforce the path-safety contract. Legacy un-scoped routes unchanged. 11 new tests in `tests/web/test_scope_artefacts.py`; **439 Python tests passing**.
- [x] **2.7-C — SPA loader scope-awareness** (commit `b674186`): `api/loader.ts` exposes `apiListScopes(runId)` and `loadModulePerScope<T>(module)`. Three-mode dispatch (static / single-scope flat / multi-scope per-scope) keeps the existing single-payload loaders untouched so Phase 3 UI pages opt in incrementally. 5 new vitest cases; **21 vitest tests passing**, `tsc --noEmit` clean.

---

## Phase 3 — Unified UI polish

- [x] `EstateSummaryCard` on Dashboard (scope count, source breakdown, readiness rollup)
- [x] Per-source/per-scope filter pills across Dashboard, Recommendations, Runbook *(Recommendations done in commit `a015af8`; Runbook done in commit `1689928`; Dashboard pipelines panel done in commit `76e9810`)*
- [x] "Estate Migration Plan" PDF export *(existing `/report/print` route + `@media print` styles deliver this; surfaced from EstateOverview today)*
- [x] Runs history columns: scope count + source-type chips
- [x] Re-record user-guide getting-started Mermaid *(commit `a015af8` — source-type branch, multi-scope fan-out, per-scope artefacts, scope filter pill node)*
- [ ] Package rename (per D2) with deprecation shim — **deferred to standalone PR**. Renaming `src/synapse_migration_analyzer/` → `src/usma/` touches hundreds of import sites across analyzers, tests, the FastAPI app, and the SPA's API contracts. Doing it inside a multi-feature PR risks silently breaking artefact JSON keys. Plan: dedicated PR that (a) does the directory move, (b) global-replaces `from synapse_migration_analyzer` → `from usma`, (c) adds a thin `src/synapse_migration_analyzer/__init__.py` that re-exports `from usma import *` with a `DeprecationWarning`, (d) bumps the manifest version, (e) runs the full test suite green.
- [x] Optional source extras registered: `[databricks]`, `[sapbw]`, `[ssis]` (per D6)
- [x] CLI `usma` entry point registered alongside `sma`

### Phase 3 exit criteria
- [x] Multi-source story feels first-class, not bolted on *(source-platform alignment slices A–D landed 2026-05-20 — see change log)*

---

## Phase 4 — Databricks

- [x] `sources/databricks/provider.py` — Databricks REST + `databricks-sdk` *(Slice 4-A, commit `4e65dbc`)*
- [x] AAD SP auth where supported; PAT fallback in `creds.extras` *(Slice 4-A)*
- [x] New module `modules/databricks_workflows/` (keep `pipelines` separate per D5) *(Slice 4-B, commit `5cff105`)*
- [x] Cost: DBU-based attribution model *(Slice 4-C, commit `bba5cbc` — ARM-id classifier recognises Databricks workspaces; cost module widened to support DATABRICKS)*
- [x] Frontend: Databricks tab + config fields *(Slice 4-D, commit `3a22c3b` — third radio in `Configuration.tsx`, `discover_databricks_workspaces` route, `MODULES_BY_SOURCE.databricks`)*
- [x] `docs/user-guide/21-databricks.md` *(Slice 4-E, commit `d4d67b3` — numbered 21 to slot in after `20-data-factory.md`)*
- [x] Run history + vCore-hour collection *(Slice 4-F, commit `5d594bd` — `iter_workflow_runs`, `est_cu_hours_fabric_spark` per workflow / interactive cluster, 7d/28d windows)*
- [x] Dashboard: workflow vCore-hr tile + 28d chart + 24h chart + interactive-cluster rollup *(Slice 4-F UI, commits `3cb6cf3`, `b01d2bc`, `7da4e2a`, `562b890`, `ebc3737`)*
- [x] Backend resolution fix for shallow-listed jobs + ephemeral clusters *(Slice 4-F-2, commit `7a26cd8` — `expand_tasks=True`, `jobs.get()` fallback, `clusters.get()` post-mortem cache gated by `SMA_DATABRICKS_CLUSTER_POSTMORTEM`)*
- [x] Estate Overview: roll Databricks Spark CU into `projected_fabric_cu` + derive `recommended_fabric_sku` *(commit `bc190b5` — new `_databricks_daily_cu` helper in `web/estate.py`, lazy-imported `_smallest_sku_covering`)*

### Deferred to Phase 4.6
- [ ] Delta Live Tables (DLT) pipelines collection + rules
- [ ] Unity Catalog inventory (tables / views / volumes / models)
- [ ] Per-cluster cost attribution via `billing/list-usage` REST endpoint

---

## Phase 4.7 — Databricks on AWS (multi-cloud expansion)

**Goal:** extend the Databricks source to cover **Databricks on AWS** workspaces alongside the existing Azure path, without forking the `databricks_workflows` module. Establishes the multi-cloud pattern that Databricks-on-GCP can adopt verbatim in a later phase.

**Approach — Option A (locked 2026-05-27):**
- Keep `SourceType.DATABRICKS = "databricks"` stable (no enum churn, manifest v2 artefacts remain readable).
- Add a `platform: Literal["azure", "aws", "gcp"]` discriminator stored at `SourceDescriptor.extras["platform"]` (defaults to `"azure"` when absent for backwards compatibility with v2 manifests written before 4.7).
- Introduce a sibling provider `DatabricksAwsProvider` next to the existing `DatabricksProvider` (renamed conceptually to "Azure Databricks provider" — class name stays for git-history hygiene). The Phase-4 provider is unchanged on disk; the new provider lives in `sources/databricks/aws_provider.py`.
- `modules/__init__.py::_databricks_workflows` dispatches to the correct provider based on `descriptor.extras["platform"]`. The entire `modules/databricks_workflows/` tree is **untouched** — `databricks.sdk.WorkspaceClient` is cloud-agnostic and `clusters.list_node_types()` populates AWS instance-type vCPU maps at runtime.

**What stays unchanged (reused as-is):**
- `modules/databricks_workflows/collector.py` — Jobs 2.x API, Clusters API, post-mortem cluster lookups, serverless task heuristics are all cross-cloud.
- `modules/databricks_workflows/run_stats.py` — `node_type_id` is opaque; AWS instance types (`m5d.large`, `i3.xlarge`, etc.) populate the vCPU map identically to Azure.
- `modules/databricks_workflows/fabric_compat.py`, `models.py`, `reporting.py`, `analyzer.py` — zero changes.
- Fabric CU-h projection (1 CU = 2 vCores) and Estate Overview rollup (`_databricks_daily_cu` in `web/estate.py`).
- `MODULE_SPECS` entries — `databricks_workflows`, `cost`, `fabric_mapping` already declare `supports={DATABRICKS}` and that's still correct.

### Slice 4.7-A — platform discriminator + descriptor plumbing

- [ ] `sources/__init__.py` — extend `SourceDescriptor` docstring to call out the `platform` extras key as a contractual field for Databricks descriptors; add a tiny `databricks_platform(descriptor) -> Literal["azure","aws","gcp"]` helper that returns `extras.get("platform", "azure")` so callers don't open-code the lookup.
- [ ] `sources/databricks/__init__.py` — re-export both providers; keep `DatabricksProvider` (Azure) as the registered default for `SourceType.DATABRICKS` so legacy manifests / env-var bootstraps continue to resolve.
- [ ] `reporting/run_manifest.py` — when serialising a Databricks `ArtifactRecord`, include `platform` in the per-scope `scopes[]` block so downstream tooling (Diff, Dashboard chips) can label rows correctly. Default to `"azure"` on read for v2 manifests written pre-4.7.
- [ ] `tests/sources/test_source_descriptor.py` — assert `platform` defaults to `"azure"` and round-trips through extras unchanged.
- [ ] `tests/test_manifest_v2.py` — fixture proving a pre-4.7 v2 manifest reads back with `platform="azure"` injected.

### Slice 4.7-B — `DatabricksAwsProvider`

- [ ] `sources/databricks/aws_provider.py` — new file. `class DatabricksAwsProvider(BaseSourceProvider)` with `type = SourceType.DATABRICKS`, `display_name = "Databricks on AWS"`.
 - **Auth (in priority order, all driven by `Credentials.extras`):**
 1. **OAuth M2M with service-principal client at workspace or account level** (preferred — keyless rotation, native `databricks-sdk` support). Read `DATABRICKS_CLIENT_ID` + `DATABRICKS_CLIENT_SECRET` from `creds.extras`; construct `WorkspaceClient(host=, client_id=, client_secret=, auth_type="oauth-m2m")`.
 2. **Personal Access Token (PAT)** — fallback. Already supported today via `creds.extras["DATABRICKS_TOKEN"]`; reused verbatim.
 3. **AWS instance profile** (EC2-hosted analyzer host) — `WorkspaceClient(host=, auth_type="databricks-cli")` with the environment-provided profile; documented but not required for alpha.
 - **Discovery (`discover`):** two modes.
 - **Account-API mode** (auto-discovery): when `DATABRICKS_ACCOUNT_ID` + account-level OAuth credentials (`DATABRICKS_ACCOUNT_CLIENT_ID` / `..._SECRET`) are present, call `AccountClient.workspaces.list()` and emit one descriptor per workspace.
 - **Explicit-config mode** (default for alpha — matches BigQuery's manual-project pattern pre-OAuth): user supplies `DATABRICKS_HOST` directly; `discover()` returns a single descriptor synthesised from the host.
 - **`validate`:** drop the ARM Reader probe; keep only `current_user.me()` as the data-plane health check.
 - **`make_clients`:** return a `DatabricksClientBundle` with `subscription_id=None`, `resource_group=None`, `workspace_url=<aws-host>`, `workspace_name=<deployment-name-from-host>`, `workspace_id=<numeric-id-from-host-or-`me()`>`, `pat_token=<resolved-token>`.
- [ ] `sources/databricks/provider.py` — no behavioural change. Add a one-line guard at the top of `discover()` that emits an empty list (and a warning) if `descriptor.extras["platform"] == "aws"` accidentally lands here (defence in depth).
- [ ] `tests/sources/databricks/test_aws_provider.py` — new file mirroring `test_provider.py`: fake `WorkspaceClient`, fake `AccountClient`, asserts both auth paths and both discovery modes; asserts that a synthesised descriptor carries `extras["platform"] == "aws"`.

### Slice 4.7-C — backend glue

- [ ] `config.py::_legacy_databricks_scope` — branch on the presence of `AZURE_SUBSCRIPTION_ID`. AWS branch reads `DATABRICKS_HOST` directly, builds a descriptor with `id=<workspace-url>` (no ARM ID), `subscription_id=None`, `resource_group=None`, `extras={"platform": "aws", "workspace_url": <host>, ...}`. Honour `SMA_DATABRICKS_PLATFORM=aws` as an explicit override when both env shapes are present.
- [ ] `cli.py::_apply_scope_flags` — extend the parser to accept `databricks-aws:<host-or-deployment-name>` as a `--scope` value alongside the existing ARM-prefix mapping. The Azure `Microsoft.Databricks/workspaces` mapping stays; the new AWS branch yields a descriptor with `extras["platform"]="aws"`.
- [ ] `doctor.py::aad_types` — gate the AAD classification on platform. Replace the static set lookup with a predicate `_is_aad_backed(scope) -> bool` that returns `True` for Synapse / ADF and for Databricks **only when** `extras.get("platform", "azure") == "azure"`. AWS Databricks scopes get a separate check group asserting OAuth M2M or PAT presence.
- [ ] `modules/__init__.py::_databricks_workflows` — dispatch by `descriptor.extras["platform"]`: `"aws"` → `DatabricksAwsProvider().make_clients(...)`, default → existing `DatabricksProvider`. Five-line change.
- [ ] `tests/test_cli_scope_flags.py` — extend with an `databricks-aws:` parse case.
- [ ] `tests/test_doctor.py` — assert AWS Databricks scopes are validated via the data-plane check path, not the AAD path.

### Slice 4.7-D — SPA Configuration sub-radio

- [ ] `web/schemas.py` — widen `AzureConfigPublic` / `Update` with an optional `databricks_platform: Literal["azure", "aws"] | None` field (default `"azure"` on read; nullable in the wire schema so the existing single-platform clients continue to validate).
- [ ] `web/config_io.py` — read/write the new key; gate the existing `discover_databricks_workspaces` Azure-ARM path on `databricks_platform == "azure"`.
- [ ] `web/api/config.py` — new route `POST /api/config/discover-databricks-aws-workspaces` calling `DatabricksAwsProvider().discover(...)`. When `DATABRICKS_ACCOUNT_ID` is unset, the route short-circuits to a single-host echo (matches the explicit-config discovery mode).
- [ ] `web/src/pages/Configuration.tsx` — when the Databricks radio is selected, render a nested **Azure / AWS** sub-radio. AWS branch hides the workspace dropdown and shows three text fields (`DATABRICKS_HOST`, `DATABRICKS_CLIENT_ID`, `DATABRICKS_CLIENT_SECRET`) plus a "Use PAT instead" toggle that swaps in a single `DATABRICKS_TOKEN` field. `discoverFor("databricks")` dispatches to the right backend route based on the sub-radio.
- [ ] `web/src/api/loader.ts` — `apiDiscoverDatabricksAwsWorkspaces()` typed function (no generic dispatcher per the Phase-1.5 supersede note).
- [ ] `web/src/pages/Run.tsx` — no change. `MODULES_BY_SOURCE.databricks` already covers the AWS path because `SourceType` is unchanged.
- [ ] `web/src/components/sourceTypes.ts` — extend the `scopeLabel()` helper to return `"AWS Databricks workspace"` when `extras.platform === "aws"`; surfaces in scope chips on Dashboard / Runs history / Diff.
- [ ] Vitest: `Configuration.test.tsx` assertion that selecting AWS swaps the form fields; loader test for the new endpoint.

### Slice 4.7-E — docs

- [ ] `docs/user-guide/21-databricks.md` — add a new top-level section **"Databricks on AWS"** covering: platform discriminator, AWS auth options (OAuth M2M preferred, PAT fallback, instance profile), Account-API vs explicit-host discovery, the `databricks-aws:` CLI scope prefix, current limits (no cost rollup until Phase 4.7.5, PrivateLink caveat).
- [ ] `QUICKSTART.md` §0.0.8 — add a "Databricks on AWS" sub-table beside the existing Azure Databricks role table, listing the [account-admin OAuth M2M setup](https://docs.databricks.com/aws/en/dev-tools/auth/oauth-m2m) and [workspace-level OAuth flow](https://docs.databricks.com/aws/en/dev-tools/auth/) docs, plus the [PrivateLink front-end ingress](https://docs.databricks.com/aws/en/security/network/classic/privatelink) caveat for analyser host placement.
- [ ] `.env.example` — add commented-out `DATABRICKS_HOST=`, `DATABRICKS_CLIENT_ID=`, `DATABRICKS_CLIENT_SECRET=`, `DATABRICKS_ACCOUNT_ID=`, `SMA_DATABRICKS_PLATFORM=aws` block under the existing Azure-Databricks env block.
- [ ] `docs/architecture/sources.md` — note the platform-discriminator pattern under the Databricks heading; flag it as the template for GCP-Databricks in a future phase.
- [ ] `docs/adr/` — new ADR-0005 "Databricks multi-cloud via descriptor discriminator" recording the Option A vs Option B trade-off (manifest-stability vs enum-purity) and why Option A won.

### Slice 4.7-F — pyproject extras + dependency check

- [ ] `pyproject.toml` — confirm `[databricks]` extra still pins `databricks-sdk` at a version that supports OAuth M2M (`>=0.20.0`). If the current pin is older, bump it. No new top-level deps required — `databricks-sdk` is already cross-cloud at the API surface.
- [ ] Smoke test: install `pip install -e .[databricks]` into a fresh venv, run `python -c "from databricks.sdk import AccountClient; AccountClient"` to verify the account-level client is importable without extras.

### Phase 4.7 exit criteria

- [ ] User can declare an AWS Databricks workspace as a scope on a run (`--scope databricks-aws:<host>` CLI flag **and** SPA Configuration AWS sub-radio path).
- [ ] `analyze-all` produces the same `databricks_workflows.json` artefact shape on AWS as it does on Azure — workflows, clusters, run history, vCore-hour rollups, Fabric Spark CU projection.
- [ ] Estate Overview's `projected_fabric_cu` rolls AWS Databricks contribution in via the unchanged `_databricks_daily_cu` helper.
- [ ] Dashboard's Databricks section labels AWS scopes correctly ("AWS Databricks workspace" chip on Estate / Recommendations / Runbook filter pills).
- [ ] Manifest v2 written on a run with at least one AWS scope round-trips through the v1→v2 upgrader and back into the SPA without information loss.
- [ ] All existing 524+ tests still green; ~12 new tests for AWS provider / glue / SPA loader.

### Deferred to Phase 4.7.5 (post-alpha)

- [ ] **AWS cost attribution.** `modules/cost/` is Azure-Cost-Management-only today. AWS Databricks billing flows through (a) AWS Cost & Usage Reports (CUR) S3 export — heaviest lift, requires Athena or DuckDB query layer; (b) the [Databricks account-level Usage API](https://docs.databricks.com/api/account/billableusage/download) returning monthly CSVs of DBU usage. Recommend option (b) for the first cut — add `modules/cost/databricks_aws_billing_client.py` reading account-id-scoped usage CSVs, mirror the shape of `gcp_cost_client.py`. Until this ships, the AWS Databricks Dashboard tile renders "Cost: unavailable on AWS — Phase 4.7.5" caveat (parallel to Phase 5's pre-cost BigQuery caveat).
- [ ] **Account-API auto-discovery without explicit account-id.** Today the AWS path requires the user to supply `DATABRICKS_ACCOUNT_ID`. An OAuth-browser flow that lists the user's visible accounts is possible but pulls in a Flask redirect step (mirrors the deferred BigQuery interactive-OAuth path). Track separately.
- [ ] **GCP Databricks.** Once 4.7 ships, GCP Databricks support is a ~1-day follow-on: add `DatabricksGcpProvider`, dispatch on `platform == "gcp"`, reuse the entire collector module. Tracked under Phase 6+.
- [ ] **PrivateLink-only workspace probe.** Add a `doctor.py` check that pings the configured host from the analyser machine and reports a clear error when the host is unreachable due to front-end PrivateLink ingress restrictions.

---

## Phase 5 — Google BigQuery

Goal: Add Google BigQuery as a third cloud source alongside Synapse + Databricks. Mirrors the Databricks slice shape (Phase 4) so the UI/CLI/manifest plumbing already in place can be reused.

**SDK assessment** — [`constantino-casado/GCP_logs`](https://github.com/constantino-casado/GCP_logs) reviewed 2026-05-20 and **declined as a dependency**:
  - GPL-3.0 licensed (our repo is MIT — incompatible without relicensing the whole project).
  - Packaged as a Tkinter **desktop app**, not a library (only `gcp-bigquery-logs = gcp_logs_desktop.gui:main` is exported; no public Python API surface, GUI glue intermixed with the data path).
  - 4 days old, 0 stars, single author + Copilot — no maintenance signal.
  - Scope is audit-log-only (`jobservice.jobcompleted`, last 30 days, hard-coded filter). We need dataset/table inventory + slot-hour attribution + scheduled-query / Dataform / Composer enumeration on top.
  - **Useful as a reference implementation** for the audit-log Cloud Logging filter expression and the BigQuery audit-log field-flattening logic (`processor.py`, `filters.py`, `gcp_logging.py`) — patterns to mirror in our own collector, not code to vendor.

**Decision:** depend directly on the first-party SDKs (`google-cloud-bigquery`, `google-cloud-resource-manager`, `google-cloud-logging`, `google-auth`) under a new `[bigquery]` optional extra. Reuse the audit-log filter expression + field-flattening shape from `GCP_logs` as inspiration, attributed in the source file header.

### Scope

**Auth model — locked 2026-05-20:** **Application Default Credentials (ADC) only.** `google.auth.default()` resolves in order: (1) `GOOGLE_APPLICATION_CREDENTIALS` env var → service-account JSON key file, (2) `gcloud auth application-default login` user creds, (3) Workload Identity Federation config (the keyless / AAD-federated path), (4) GCE / GKE / Cloud Run metadata server. USMA does not implement OAuth interactive flow, API keys, or end-user refresh-token JSON. The `Credentials` Pydantic model (AAD-shaped) is **not used** by `BigQueryProvider`; the Protocol docstring is widened to allow `Credentials | None` so providers may ignore it. Optional escape hatch: `creds.extras["gcp_service_account_json"]` (filesystem path) populates `GOOGLE_APPLICATION_CREDENTIALS` for the duration of the call.

**Env-var contract — locked 2026-05-20:**
- `SMA_SOURCE_TYPE=bigquery`
- `SMA_GCP_PROJECT_ID=<project-id>` (becomes `descriptor.name` / scope slug)
- `GOOGLE_APPLICATION_CREDENTIALS=<path>` (standard ADC env var — preferred over a custom `SMA_GCP_SA_KEY_PATH`)
- `SMA_GCP_BILLING_ACCOUNT=<id>` (optional, for the deferred 5-H cost slice)

**Enum value — locked 2026-05-20:** `SourceType.BIGQUERY = "bigquery"` to match the SPA `sourceTypes.ts` union (already in place).

- [x] Add `SourceType.BIGQUERY = "bigquery"` to `sources/__init__.py`; **add `sources/bigquery/` alongside the existing `sources/sap_bw/` stub** (SAP BW Phase-0 scaffolding remains for later Phase 6+ revival per change log). *(shipped Slice 5-A — commits `cc5f446` pre-flight + this commit; `sources/bigquery/` self-registers via `__init__.py` import chain)*
- [x] `sources/bigquery/provider.py` — ADC by default via `google.auth.default()`; service-account JSON key path via `creds.extras["gcp_service_account_json"]` as escape hatch. `discover()` lists visible projects via Cloud Resource Manager (`google-cloud-resource-manager`); `validate()` probes `bigquery.Client.get_service_account_email()` + a `logging_v2.Client.list_entries(filter_="severity>=ERROR", page_size=1)` no-op call. *(shipped Slice 5-A — `BigQueryProvider` + `BigQueryClientBundle` + `_maybe_inject_sa_key` context manager; ARG002 noqa on the ignored Azure `subscription_id` kwarg)*
- [x] New optional extra `[bigquery]` in `pyproject.toml` pinning `google-cloud-bigquery>=3.25`, `google-cloud-resource-manager>=1.13`, `google-cloud-logging>=3.12`, `google-auth>=2.30`. *(shipped pre-flight commit `cc5f446`; `[dev]` extra now rolls up `[databricks]` + `[bigquery]` so the full test suite installs in one command)*
- [x] New module `modules/bigquery_workloads/` (sibling of `databricks_workflows` per D5): `models.py` (`Dataset`, `Table`, `Routine`, `ScheduledQuery`, `BigQueryJob`, `BigQueryWorkloadsAnalysis`), `collector.py` (datasets/tables via `bigquery.Client`, scheduled queries via Data Transfer Service, jobs/slot-hours via Cloud Logging audit logs — pattern lifted from `GCP_logs`), `analyzer.py` (`BigQueryWorkloadsAnalyzer.for_descriptor()`), `reporting.py`, `fabric_compat.py`. *(shipped Slice 5-B — also registered in `MODULE_REGISTRY` and `MODULE_SPECS["bigquery_workloads"]` with `supports={BIGQUERY}`; `[bigquery]` extra widened with `google-cloud-bigquery-datatransfer>=3.15`; 23 new tests)*
- [x] Slot-hour → vCore-hour → Fabric Spark CU conversion in `bigquery_workloads/run_stats.py` (mirrors `databricks_workflows/run_stats.py`; document the slot ≈ vCore assumption with a caveat string). *(shipped Slice 5-C — `SLOT_HOURS_TO_VCORE_HOURS=0.5`, `SLOT_HOURS_TO_CU_HOURS=0.25`, `SLOT_TO_CU_CAVEAT` surfaced by the analyzer; rolling windows 7/14/28/90)*
- [x] `modules/fabric_mapping/rules.py::rules_for_bigquery_workloads(...)` — inventory rec; per-job-type bucket (`SELECT` / `LOAD` / `EXTRACT` / `COPY` / `QUERY_SCRIPT` / `ML_*`); scheduled-query → Fabric pipeline / Data Factory mapping; routine (UDF / stored proc / table function) signals; partitioning + clustering carry-over hints. *(shipped Slice 5-C — also registered in `fabric_mapping/analyzer.py::_INPUT_FILES` + `_RULES`)*
- [x] `MODULE_SPECS["cost"].supports`, `MODULE_SPECS["fabric_mapping"].supports`, and a new `MODULE_SPECS["bigquery_workloads"]` registered with `supports={BIGQUERY}`. *(`fabric_mapping` widened to BIGQUERY in Slice 5-C; `bigquery_workloads` spec landed in Slice 5-B; `cost` widened to BIGQUERY in Slice 5-H)*
- [x] Cost: GCP needs a separate `modules/cost/gcp_cost_client.py` reading BigQuery billing-export tables (`gcp_billing_export_v1_*`) or the Cloud Billing API. Track as a sub-slice once the workloads module ships. *(shipped Slice 5-H)*
- [x] SPA: fourth radio in `Configuration.tsx` (Synapse / ADF / Databricks / BigQuery); helpers (`scopeNoun`, `scopeLabel`, `discoverFor`) extended; `Run.tsx` `MODULES_BY_SOURCE.bigquery = {bigquery_workloads, cost, fabric_mapping}` (cost added in Slice 5-H per the original manifest spec); per-source guidance line. *(shipped Slice 5-D + Slice 5-H)*
- [x] Backend: `web/schemas.py` widens `source_type` Literals; `web/config_io.py::discover_bigquery_projects()` + a `POST /api/config/discover-bigquery-projects` route; `validate_config_live` branch. *(shipped Slice 5-D)*
- [x] `docs/user-guide/22-bigquery.md` mirrors `21-databricks.md` (auth flow, scopes, collected shapes, limits, roadmap). *(shipped Slice 5-E)*
- [x] Verify the BigQuery CU contribution rolls into `projected_fabric_cu` via a new `_bigquery_daily_cu` helper in `web/estate.py` (sibling of `_databricks_daily_cu`). *(shipped Slice 5-F)*
- [x] SPA Dashboard: `BigQueryWorkloadsSection` rendered between Databricks and Serverless using the same idiom as the other source sections — 3-card headline (jobs / slot-hours / Fabric CU), side-by-side daily + last-24h hourly bar charts, GCS storage view (per-dataset logical bytes / row count / table count), top-users + statement-type + top-tables breakdowns, sqlglot query-features table, caveats + errors footers. Standalone `/bigquery` route removed. *(shipped Slice 5-J)*

---

## Phase 7 — Snowflake

**Status: 🟢 Complete (alpha) — 2026-05-30.** All ten slices (7-A → 7-J) shipped. Production hardening fixes for IMPORTED-PRIVILEGES-only roles, `USAGE_IN_CURRENCY_DAILY` unavailability (credits-only fallback), PUBLIC-role detection, refresh-token role binding, and scope-aware cost-finding wording all landed in the days that followed. Snowflake-on-AWS is the validated path; Azure/GCP variants reuse the provider via `extras["platform"]` discriminator. **Deferred:** ADR-0007 (auth model write-up — key-pair JWT vs External-OAuth-via-Entra), Snowpark-driven deep scope inspection, fine-grained `SNOWFLAKE.ACCOUNT_USAGE` least-privilege role recipe in the user guide.

Goal: Add Snowflake as a fourth cloud source alongside Synapse / ADF / Databricks / BigQuery. Mirrors the Databricks (Phase 4) and BigQuery (Phase 5) slice shape so the existing UI/CLI/manifest plumbing carries Snowflake without architectural changes.

**Feasibility review — 2026-05-28.** Audit of the Databricks integration surface (provider, collector, fabric compat, cost-skip, CLI scope shortcut, SPA Configuration radio + auth panel, env-var family, ADR, user-guide, ~21 test files) concluded: the plugin seams (`SourceProvider`, `SourceDescriptor` + `extras`, `MODULE_REGISTRY`, `fabric_mapping/rules.py` rule-per-artifact, Estate Overview cloud bucketing) hold cleanly for Snowflake. No prerequisite refactor. Hardcoded `"databricks"` strings (CLI scope parser, SPA radio, env-var family, doctor branches, labels, artifact filenames) get parallel `"snowflake"` additions next to them — mechanical, not structural. **Estimated effort:** ~3–4 focused weeks for full Databricks parity, **assuming auth model is decided early (Slice 7-A) and the compat matrix is signed off by a domain expert (Slice 7-C)**.

### Scope

**Auth model — to be locked in Slice 7-A.** Candidate flows:
  1. **Key-pair JWT** (Snowflake's recommended programmatic auth: RSA keypair, public key registered on the user, private key signs a JWT per request). Maps cleanly to `Credentials.extras["snowflake_private_key"]` + `extras["snowflake_private_key_passphrase"]`. No browser flow needed — best for CI.
  2. **OAuth via Entra/Okta** (Snowflake supports external OAuth IdPs). Reuses the existing AAD `ClientSecretCredential` if Snowflake is configured with Entra as the OAuth IdP. Best for enterprise SSO alignment, but every customer's IdP wiring is different.
  3. **Username/password + MFA** (rejected for non-interactive use).
  4. **PAT** (Snowflake doesn't have first-class PATs equivalent to Databricks; the closest is a session token issued after key-pair JWT auth — not a separate flow).

  **Default plan:** key-pair JWT primary; document the External-OAuth-via-Entra path in the user guide as a follow-up if customer demand warrants. ADR-0007 to capture the decision.

**Env-var contract — to be locked in Slice 7-A:**
  - `SMA_SOURCE_TYPE=snowflake`
  - `SNOWFLAKE_ACCOUNT=<orgname>-<accountname>` (becomes `descriptor.id` / scope slug)
  - `SNOWFLAKE_USER=<username>`
  - `SNOWFLAKE_PRIVATE_KEY=<pem-string-or-path>` (key-pair JWT)
  - `SNOWFLAKE_PRIVATE_KEY_PASSPHRASE=<passphrase>` (optional)
  - `SNOWFLAKE_ROLE=<role>` (default `ACCOUNTADMIN` only for discovery; least-priv `USAGE` on `SNOWFLAKE.ACCOUNT_USAGE` for production analysis)
  - `SNOWFLAKE_WAREHOUSE=<warehouse>` (warehouse to run the analyzer's own queries against)
  - `SMA_SNOWFLAKE_PLATFORM={aws|azure|gcp}` (cloud discriminator persisted as `extras["platform"]` — same pattern as Databricks 4.7; defaults to `aws` since that is Snowflake's most common deployment)

**Enum value — to be locked in Slice 7-A:** `SourceType.SNOWFLAKE = "snowflake"`.

**Optional extra:** `[snowflake] = ["snowflake-connector-python>=3.7", "snowflake-snowpark-python>=1.20"]` in `pyproject.toml` (`snowpark` only if scope inspection needs it; can be deferred to a sub-slice).

### Slices

- [x] **7-A — Pre-flight.** *(2026-05-28)* `SourceType.SNOWFLAKE = "snowflake"` enum value already present from the original D6 reservation. New `sources/snowflake/{__init__.py,provider.py}` ship `SnowflakeProvider` stub (registered in `SOURCE_REGISTRY[SNOWFLAKE]`, `required_env=("SNOWFLAKE_ACCOUNT","SNOWFLAKE_USER","SNOWFLAKE_PRIVATE_KEY","SNOWFLAKE_WAREHOUSE")`, `discover`/`make_clients` raise `NotImplementedError("…Slice 7-B")`, `validate` returns `[]`). New `[snowflake]` extra in `pyproject.toml` pinning `snowflake-connector-python>=3.7` + `cryptography>=42.0`; `[dev]` extra widened to include both so the test suite installs in one command. SPA `SourceTypeId` already lists `"snowflake"` (lifted to `sourceTypes.ts` during 1.5 cleanup); `EstateSummaryCard.TYPE_LABEL` already maps it to `"Snowflake"`. 5 new tests in `tests/sources/snowflake/test_provider.py` (registry lookup, required-env contract, stub discover, stub make_clients, validate-returns-empty). **775 Python tests passing.** ADR-0007 (Snowflake auth — key-pair JWT primary, External-OAuth-via-Entra deferred) deferred to Slice 7-G when the user-guide chapter lands; manifest §Scope captures the locked contract in the interim.
- [x] **7-B — `SnowflakeProvider`.** *(2026-05-28)* Real implementation lands in `sources/snowflake/provider.py`. `discover()` returns a single `SourceDescriptor` for the configured account (Snowflake scope-unit is the account, not the database — unlike BigQuery / GCP project enumeration, a key-pair JWT authorises exactly one account); `validate()` runs three live probes (key-pair JWT resolution surfacing `account` + `user`, `CURRENT_VERSION()` to confirm data-plane reachability, `SELECT 1 FROM SNOWFLAKE.ACCOUNT_USAGE.WAREHOUSE_METERING_HISTORY LIMIT 1` to confirm the role has the `ACCOUNT_USAGE` privileges every later slice depends on); `make_clients()` returns a `SnowflakeClientBundle` whose zero-arg `connect()` opens a fresh `snowflake.connector` connection on each call (each analyzer pass uses `contextlib.closing` so sessions don't leak). `extras["platform"]` is populated from `CURRENT_REGION()` prefix sniffing (`AWS_*` → aws, `AZURE_*` → azure, `GCP_*` → gcp), with `SMA_SNOWFLAKE_PLATFORM` env fallback and a final `"aws"` default. `extras["region"]` carries the raw region string; `extras["edition"]` best-effort from `ACCOUNT_USAGE.ACCOUNT_PROPERTIES`. Auth resolution reads `Credentials.extras` first (`snowflake_account` / `snowflake_user` / `snowflake_private_key` / `snowflake_private_key_passphrase` / `snowflake_role` / `snowflake_warehouse`) then falls back to `SNOWFLAKE_*` env vars. Private key may be inline PEM or a filesystem path — either way loaded via `cryptography.hazmat` and re-serialised to PKCS8 DER for the connector. CLI gains `--scope snowflake:<account>[@<platform>]` shortcut (mirrors the `databricks-aws:<host>` pattern); empty account / unknown platform produce `click.UsageError`. New `host_to_descriptor(account, *, platform)` helper exported from `sources/snowflake/__init__.py`. 27 new tests across `tests/sources/snowflake/test_provider_slice_7b.py` (5 credential resolution paths, 4 discover paths including all three cloud platforms + env fallback + aws default, 3 validate paths, 2 make_clients paths, 4 host_to_descriptor paths, 1 missing-extra ImportError) + 5 in `test_cli_scope_flag.py` (bare account, explicit platform aws/azure/gcp, empty-account rejection, unknown-platform rejection, mixed-with-azure). **796 Python tests passing.**
- [x] **7-C — `snowflake_workloads` module.** *(2026-05-28)* `modules/snowflake_workloads/` ships `models.py` (`Warehouse`, `Database`, `Schema`, `Table`, `Routine`, `Stage`, `Stream`, `Task`, `Pipe`, `SnowflakeJob`, plus placeholder `SnowflakeWarehouseWindowStats` / `SnowflakeJobWindowStats` shapes whose math lands in 7-D, and `SnowflakeWorkloadsAnalysis` carrying `account` / `platform` / `region` / `edition`); `fabric_compat.py` (`classify_table` for TABLE / VIEW / MATERIALIZED_VIEW / EXTERNAL / DYNAMIC / ICEBERG / TEMPORARY / TRANSIENT, `classify_routine` for FUNCTION / PROCEDURE × SQL / JavaScript / Python / Java / Scala matrix with JavaScript + Java verdicts unsupported, `classify_object` for STAGE / STREAM / TASK / PIPE); `collector.py` (`SnowflakeWorkloadsCollector` wrapping the zero-arg `connect` callable from `SnowflakeClientBundle`; `SHOW WAREHOUSES` / `SHOW DATABASES` / `SHOW SCHEMAS IN DATABASE` / `SHOW TABLES` / `SHOW VIEWS` / `SHOW USER FUNCTIONS` / `SHOW PROCEDURES` / `SHOW STAGES` / `SHOW STREAMS` / `SHOW TASKS` / `SHOW PIPES` for inventory; `SNOWFLAKE.ACCOUNT_USAGE.QUERY_HISTORY` for the configurable-window job pull); `analyzer.py` (`SnowflakeWorkloadsAnalyzer.for_descriptor()`, env-driven `SMA_SNOWFLAKE_JOB_LOOKBACK_DAYS` / `SMA_SNOWFLAKE_JOB_LIMIT` / `SMA_SNOWFLAKE_INCLUDE_SYSTEM_DBS`, system-DB + `INFORMATION_SCHEMA` filtering, per-schema error isolation, support-label roll-ups); `reporting.py` (`snowflake_workloads.json` + 12 per-row CSVs + markdown narrative). `MODULE_REGISTRY` + `MODULE_SPECS` widened. Compat matrix (Snowpark Python → partial, JS / Java UDFs → unsupported, streams → partial, tasks → partial, dynamic tables → partial, Iceberg → partial, semi-structured types deferred to 7-D rule-table) implemented in `fabric_compat.py` and gated behind 24 new tests in `tests/modules/snowflake_workloads/test_analyzer.py`. Credit → vCore-hour → CU math, `rules_for_snowflake_workloads`, and warehouse-size F-SKU recs deferred to 7-D. **820 Python tests passing.**
- [x] **7-D — Credit → vCore-hour → CU rollup.** *(2026-05-28)* `modules/snowflake_workloads/run_stats.py` ships `CREDITS_PER_HOUR_BY_SIZE` + `VCORES_BY_SIZE` (X-SMALL=1, SMALL=2, MEDIUM=4, LARGE=8, X-LARGE=16, 2X-LARGE=32, 3X-LARGE=64, 4X-LARGE=128, 5X-LARGE=256, 6X-LARGE=512), `CREDIT_TO_VCORE_HOURS=1.0` (size-class proxy), and the combined `CREDIT_TO_CU_HOURS=0.5` derived from `VCORE_HOURS_TO_CU_HOURS=0.5` so a single edit in `spark_history_client` rebalances every cloud. `populate_warehouse_size_metrics(wh)` fills `Warehouse.credits_per_hour` / `est_vcore_hours_per_hour`; `populate_job_credits(job)` fills `est_vcore_hours`, `est_credits`, `est_cu_hours_fabric_warehouse` preferring `execution_ms` over `total_elapsed_ms`. `aggregate_jobs_by_window(jobs)` emits one `SnowflakeJobWindowStats` per 7/14/28/90-day window with credit-cloud-services + bytes-scanned + Fabric CU totals. `aggregate_warehouse_metering_by_window(jobs)` rolls credits + vCore-hours + CU per warehouse per window (sorted by window then warehouse name, unattributable jobs skipped). `CREDIT_TO_CU_CAVEAT` calls out the proxy + names the planned `SMA_SNOWFLAKE_CREDIT_TO_VCORE_RATIO` calibration knob; analyzer appends it exactly once when any credit math was applied. `rules_for_snowflake_workloads` in `fabric_mapping/rules.py` emits inventory headline + per-kind unsupported / partial table rollups + routine kind×language rollup with JS / Java flagged as unsupported + tasks / streams / pipes / stages narratives + warehouse F-SKU rec via `_smallest_sku_covering(sustained_cu)` from the 7-day warehouse window + credit-caveat passthrough. `MODULE_SPECS["fabric_mapping"].supports` widened to include `SNOWFLAKE`; `fabric_mapping/analyzer.py` registers `snowflake_workloads.json` + `rules_for_snowflake_workloads`. 29 new tests across `test_run_stats.py` (22: constants doubling progression, size lookups, populate_warehouse / job branches incl. exec-vs-elapsed preference + unknown-size no-ops, window aggregation incl. empty + sorted-by-window-then-warehouse + skip-unattributable, end-to-end analyzer wiring) and `test_rules.py` (9: inventory, table rollups, routine flagging, object recs, F-SKU math `336 CU-hr / 7 days = F2`, caveat passthrough, source_type kwarg). Snowflake cost (`SNOWFLAKE.ACCOUNT_USAGE.METERING_HISTORY` + `USAGE_IN_CURRENCY_DAILY`) remains deferred to 7-H. **820 → 849 Python tests passing.**
- [x] **7-E — SPA Configuration + Run page.** *(2026-05-28)* `web/schemas.py` widens `ScopeRef.source_type` + `AzureConfigPublic.source_type` + `AzureConfigUpdate.source_type` Literals to include `"snowflake"`, and adds `snowflake_account` / `snowflake_user` / `snowflake_role` / `snowflake_warehouse` plain fields plus write-only `snowflake_private_key` / `snowflake_private_key_passphrase` secrets (collapsed to `"set"`/`"unset"` on reads) and UI-only `snowflake_platform` hint. `web/config_io.py` ships `_SNOWFLAKE_PLAIN_KEYS` / `_SNOWFLAKE_SECRET_KEYS` env-var dicts mirroring the Databricks-AWS pattern, extends `read_config` / `write_config` to round-trip the `SNOWFLAKE_*` family + `SMA_SNOWFLAKE_PLATFORM`, adds `discover_snowflake_databases()` (single-account WorkspaceSummary via `SnowflakeProvider.discover` — no AAD creds required), and adds a Snowflake branch in `validate_config_live` that bypasses both the `_REQUIRED_LIVE` AAD gate and the Synapse SQL gauntlet (mirrors the BigQuery branch). New `POST /api/config/discover-snowflake-databases` route in `web/api/config.py`. SPA: fifth radio in `Configuration.tsx` ("Snowflake" with alpha badge) feeding a new `bucket="snowflake"` form that surfaces account / user / role / warehouse text fields plus a PEM textarea + passphrase password input (same write-only contract as `databricks_client_secret`); `discoverFor()` / `scopeNoun()` / `scopeLabel()` / `discoverVerbObject()` widen to cover Snowflake; `credsReady` gate requires account + user + warehouse + private-key-set. `Run.tsx` `MODULES_BY_SOURCE.snowflake = {snowflake_workloads, fabric_mapping}` (cost added in 7-H); `ALL_MODULES` widens with `snowflake_workloads`. New `apiDiscoverSnowflakeDatabases()` loader. `ScopeFilter.tsx` `SOURCE_LABELS` and `lib/labels.ts` `MODULE_LABELS` pick up Snowflake entries. 9 new tests in `tests/test_snowflake_slice_7e.py` (read/write round-trip incl. secret collapse + clear-on-blank; `discover_snowflake_databases` success / error / no-AA `web/config_io.py` ships `_SNOWFLAKE_PLAIN_KEYS` / `_SNOWFLAKE_SECRET_KEYS` env-var dicts mirroring the Databricks-AWS pattern, extends `read_config` / `write_config` to round-trip the `SNOWFLAKE_*` family + `SMA_SNOWFLAKE_PLATFORM`, adds `discover_snowflake_databases()` (single-account WorkspaceSummary via `SnowflakeProvider.discover` — no AAD creds required), and adds a Snowflake branch in `validate_config_live` that bypasses both the `_REQUIRED_LIVE` AAD gate and the Synapse SQL gauntlet (mirrors the BigQuery branch). New `POST /api/config/discover-snowflake-databases` route in `web/api/config.py`. SPA: fifth radio in `Configuration.tsx` ("Snowflake" with alpha badge) feeding a new `bucket="snowflake"` form that surfaces account / user / role / warehouse text fields plus a PEM textarea + passphrase password input (same write-only contract as `databricks_client_secret`); `discoverFor()` / `scopeNoun()` / `scopeLabel()` / `discoverVerbObject()` widen to cover Snowflake; `credsReady` gate requires account + user + warehouse + private-key-set. `Run.tsx` `MODULES_BY_SOURCE.snowflake = {snowflake_workloads, fabric_mapping}` (cost added in 7-H); `ALL_MODULES` widens with `snowflake_workloads`. New `apiDiscoverSnowflakeDatabases()` loader. `ScopeFilter.tsx` `SOURCE_LABELS` and `lib/labels.ts` `MODULE_LABELS` pick up Snowflake entries. 9 new tests in `tests/test_snowflake_slice_7e.py` (read/write round-trip incl. secret collapse + clear-on-blank; `discover_snowflake_databases` success / error / no-AAD paths; `validate_config_live` provider routing + missing-account short-circuit; HTTP route smoke). **849 → 858 Python tests passing.** 25 SPA vitest tests still pass; `tsc --noEmit` clean.
- [x] **7-E.1 — Snowflake OAuth rewire.** *(2026-05-28)* Per ADR-0007 follow-up, replaces the key-pair JWT auth contract introduced in Slice 7-B with Snowflake's built-in OAuth integration (refresh-token grant — client_credentials is unsupported on built-in OAuth, only authorisation-code + refresh-token). New env vars: `SNOWFLAKE_OAUTH_CLIENT_ID` / `SNOWFLAKE_OAUTH_CLIENT_SECRET` / `SNOWFLAKE_OAUTH_REFRESH_TOKEN` (primary path, mint a short-lived access token at runtime against `https://<account>.snowflakecomputing.com/oauth/token-request` via stdlib `urllib.request` — same precedent as `modules/cost/fabric_pricing.py`, no new HTTP dep), plus optional `SNOWFLAKE_OAUTH_TOKEN` pre-minted access-token escape hatch for CI / break-glass. Removed env vars: `SNOWFLAKE_PRIVATE_KEY` / `SNOWFLAKE_PRIVATE_KEY_PASSPHRASE`. `provider.py`: `required_env` widened to the new 6-tuple; new `_exchange_refresh_token()` POSTs form-urlencoded `grant_type=refresh_token` and returns `access_token`; `_resolve_connect_kwargs()` returns connector kwargs with `authenticator='oauth'` + `token=<access>`; `make_clients()` re-resolves on every `connect()` so access tokens stay fresh under the ~10-min Snowflake OAuth TTL; `_load_private_key()` + `cryptography.hazmat` import dropped. `schemas.py`: `AzureConfigPublic` drops `snowflake_private_key` / `_passphrase` and adds `snowflake_oauth_client_id` plain + `snowflake_oauth_client_secret` / `_refresh_token` / `_token` collapsed to `set`/`unset`; `AzureConfigUpdate` accepts the three secrets as write-only fields. `config_io.py`: `_SNOWFLAKE_PLAIN_KEYS` + `_SNOWFLAKE_SECRET_KEYS` swapped; `discover_snowflake_databases` docstring + the `validate_config_live` Snowflake check renamed to "Snowflake OAuth token resolution". `pyproject.toml`: `[snowflake]` + `[dev]` extras drop the explicit `cryptography>=42.0` pin (the connector still depends on it transitively). SPA: `loader.ts` `AppConfig.azure` shape updated; `Configuration.tsx` replaces the PEM textarea + passphrase input with an OAuth client id text input + 3 password/textarea inputs (client secret + refresh token + optional pre-minted access token) and an updated explanatory paragraph; `credsReady` gate requires either `snowflake_oauth_token === "set"` OR (`client_id` + `client_secret` + `refresh_token` all populated). Test files rewritten across `tests/sources/snowflake/test_provider.py` + `tests/sources/snowflake/test_provider_slice_7b.py` (~30 tests including new `_exchange_refresh_token` happy / HTTP-error / missing-access_token paths + pre-minted-token-skips-exchange path + token-minted-fresh-on-every-connect path) + `tests/test_snowflake_slice_7e.py` (seed helper + persist/read/clear assertions swapped to OAuth env vars). **858 → 863 Python tests passing.** SPA vitest 25/25, `tsc --noEmit` clean. ADR-0007 to land alongside the user-guide chapter in Slice 7-G.
- [x] **7-E.2 — Browser sign-in to Snowflake.** *(2026-05-28)* Companion to 7-E.1 — adds a one-click **Sign in to Snowflake** button in the SPA Configuration page so customers can mint the long-lived `SNOWFLAKE_OAUTH_REFRESH_TOKEN` via a browser consent screen instead of running the authorisation-code dance by hand. New `web/api/snowflake_auth.py` (sibling of `gcp_auth.py` from Slice 5-I) exposes `POST /api/auth/snowflake/login` + `GET /api/auth/snowflake/status` + `POST /api/auth/snowflake/reset` registered on `prefix="/api/auth"`. `POST /login` is non-blocking: reads `SNOWFLAKE_ACCOUNT` / `SNOWFLAKE_OAUTH_CLIENT_ID` / `SNOWFLAKE_OAUTH_CLIENT_SECRET` from the configured `.env` (400 with the missing-key list if any absent), flips `_status` to `pending`, and spawns a daemon thread that picks a free localhost port via `_free_port()`, stands up a one-shot stdlib `http.server.HTTPServer`, opens the user's default browser at `https://<account>.snowflakecomputing.com/oauth/authorize?response_type=code&client_id=…&redirect_uri=http://localhost:<port>/&scope=refresh_token[+session:role:<ROLE>]`, waits up to 5 minutes for the callback, POSTs `grant_type=authorization_code` to `/oauth/token-request` via stdlib `urllib.request`, and persists the resulting `refresh_token` to the project `.env` via `dotenv.set_key("SNOWFLAKE_OAUTH_REFRESH_TOKEN", …)`. Access token from the exchange is *not* persisted (~10-min TTL — the analyzer mints fresh ones on demand via 7-E.1's `_exchange_refresh_token`). Single-flight semantics + 409 on `reset` while pending + every failure path (missing env, browser callback error, token-exchange RuntimeError, .env write OSError) recorded into `_status.error` so the UI never gets stuck on `pending`. SPA: `loader.ts` gains `apiSnowflakeAuthLogin()` / `apiSnowflakeAuthStatus()` / `apiSnowflakeAuthReset()` + matching `SnowflakeAuthState` / `SnowflakeAuthStatus` types. `Configuration.tsx` Snowflake form-block grows a **Sign in to Snowflake** button + status pill + localhost-only caveat banner directly under the OAuth client id/secret rows (gated on account + client_id present and client_secret = "set"); while `snowflakeAuth.state === "pending"` the SPA polls `/api/auth/snowflake/status` every 1500ms; on `ready` it reloads `apiGetConfig()` so the refresh-token pill flips to `set` and clears the cached `workspaces` so the existing auto-discover effect re-fires. 13 new tests in `tests/test_snowflake_slice_7e2.py` (status-starts-idle, login-rejects-when-env-missing × 2, login-success-persists-refresh-token-to-env with `set_key` round-trip + redirect_uri assertion, single-flight, `_run_flow` error capture on exchange RuntimeError + on OAuth callback error param, `_build_authorize_url` with/without role + URL-encoding, `_exchange_code_for_refresh_token` happy/missing-refresh-token paths, reset-clears-state, reset-409-while-pending). **863 → 876 Python tests passing.** SPA vitest 25/25, `tsc --noEmit` clean. ADR-0007 still pending Slice 7-G; this slice lands the UX wiring referenced there.
- [x] **7-F — Estate Overview CU rollup.** *(2026-05-28)* `_snowflake_daily_cu(snowflake)` helper in `web/estate.py` (sibling of `_databricks_daily_cu` / `_bigquery_daily_cu`); reads `warehouse_window_stats` 7-day row; sums `est_cu_hours_fabric_warehouse` across warehouses; converts to sustained CU/day. Folded into `projected_fabric_cu`. Estate Overview hyperscaler grouping picks up Snowflake's cloud automatically via the existing `_cloud_for` resolver (`extras["platform"]` already populated by 7-B). `web/src/components/EstateSummaryCard.tsx` + `ScopeFilter.tsx` `SOURCE_LABELS` entries verified present from 7-E.
- [x] **7-G — Docs.** *(2026-05-28)* `docs/user-guide/23-snowflake.md` mirrors `21-databricks.md` / `22-bigquery.md`: alpha banner, scope shape, CLI quick start, OAuth-refresh-token authentication (with redirect-URI exact-match note), IAM / role requirements (`ACCOUNTADMIN` for setup, scoped `USAGE` + `IMPORTED PRIVILEGES ON DATABASE SNOWFLAKE` for production analysis), support matrix incl. `cost` ✅ post-7-H, collected shapes, Estate Overview CU rollup section, Cost section detailing `METERING_HISTORY` + `USAGE_IN_CURRENCY_DAILY` join, limits, roadmap. New ADR-0007 (Snowflake auth — OAuth refresh-token chosen over key-pair JWT / External OAuth / PAT) and ADR-0008 (Snowflake multi-cloud via `CURRENT_REGION()` prefix sniffing). `docs/adr/README.md` index updated. `README.md` Source-Status block + modules block extend with Snowflake; `QUICKSTART.md` header + module table + auth pointer extend likewise. `docs/user-guide/README.md` chapter index gains §23.
- [x] **7-H — Cost (credit → USD).** *(2026-05-28)* New `src/usma/modules/cost/snowflake_cost_client.py` reading `SNOWFLAKE.ACCOUNT_USAGE.METERING_HISTORY` (per-warehouse credits per month) + `USAGE_IN_CURRENCY_DAILY` (storage line + cents-per-credit rate derivation from `usage_type IN ('compute','cloud services')`). `CostAnalyzer.__init__` dispatches on `scope.type == SourceType.SNOWFLAKE` to instantiate `SnowflakeCostClient(scope)`; `CostAnalyzer.run()` carries `account` through to `workspace_name` / `subscription_id` with empty `resource_group` (mirrors the BigQuery branch). `_client.filter_rows_for_scope` skipped for Snowflake (rows are already account-scoped). `MODULE_SPECS["cost"].supports` widened to include `SNOWFLAKE`. `Run.tsx::MODULES_BY_SOURCE.snowflake` widens to `{snowflake_workloads, cost, fabric_mapping}`. Status vocabulary `ok | sdk_missing | live_disabled | missing_config | empty_window | error`; honours the shared `SMA_COST_DISABLE_LIVE` kill-switch. Reuses `SnowflakeProvider.make_clients(...).connect()` so the same OAuth-refresh-token plumbing minted in 7-E.1 / 7-E.2 drives the cost reads — no new auth surface. 9 new tests in `tests/test_snowflake_slice_7h.py` (account-id required, `live_disabled` + `missing_config` short circuits, live happy-path mocking `SnowflakeProvider.make_clients` with a fake connection that returns canned `METERING_HISTORY` + `USAGE_IN_CURRENCY_DAILY` rows incl. `CLOUD_SERVICES` rollup row, `empty_window`, zero-rate degradation showing usage-but-not-cost, `CostAnalyzer` dispatch routing Snowflake to `SnowflakeCostClient`, `MODULE_SPECS["cost"].supports` snowflake assertion) + `test_module_spec.py` widened so `applicable_modules(SourceType.SNOWFLAKE) == {snowflake_workloads, cost, fabric_mapping}`. **893 Python tests passing**, 25 vitest, `tsc --noEmit` clean.
- [ ] **7-I — SPA Dashboard.** `SnowflakeWorkloadsSection` rendered between BigQuery and Serverless using the same idiom: 3-card headline (queries / credit-hours / Fabric CU), side-by-side daily + last-24h hourly bar charts coloured by warehouse, top-warehouses + top-roles + statement-type breakdowns, sqlglot Snowflake-dialect query-features table (gated on `SMA_SF_CAPTURE_QUERY_TEXT=1` to keep `QUERY_HISTORY` rows from bloating the artifact), caveats + errors footers. Standalone route not added (matches the BigQuery 5-J consolidation decision).
- [ ] **7-J — Multi-cloud parity.** Confirm `SnowflakeProvider` discovers + validates correctly on all three clouds (AWS / Azure / GCP) using `CURRENT_REGION()` prefix sniffing; persist `extras["platform"]` through the SPA-launched-run path (the `jobs.py` extras forwarding bug fix from 5.1.3 makes this automatic); verify Estate Overview hyperscaler grouping buckets Snowflake-on-Azure under Azure, Snowflake-on-AWS under AWS, etc. Smoke tests against at least one real workspace per cloud.

### Out of scope (Phase 7)

- **Snowpark container services.** Deferred — not part of the Fabric migration target story today.
- **Data Sharing / Marketplace / Cortex AI.** Inventoried (counts only) but not deep-analyzed; no Fabric equivalent.
- **Iceberg tables on external storage.** Inventoried; mapping to Fabric Lakehouse / OneLake shortcut deferred to a follow-up slice if customer demand warrants.
- **dbt artifacts.** dbt projects backed by Snowflake are out of scope here; if collected, would land in a parallel `dbt_projects` module shared across Snowflake / BigQuery / Databricks.

---

## Phase 6+ — Subsequent sources

- [ ] SAP BW *(deferred from Phase 5 on 2026-05-20 in favour of Google BigQuery; Phase-0 stubs `sources/sap_bw/` remain on disk for later revival; `pyrfc` extra already registered as `[sapbw]` per D6)*
- [ ] SQL Server / SSIS
- [ ] Snowflake → promoted to **Phase 7** on 2026-05-28; see section above.
- [ ] (others as demand emerges)

---

## Change log

| Date | Phase | Change |
|---|---|---|
| 2026-05-28 | 7 | **Phase 7 Slices 7-F + 7-G + 7-H — Estate CU rollup, docs, Snowflake cost client.** *7-F:* `_snowflake_daily_cu(snowflake)` helper added to `web/estate.py` next to `_databricks_daily_cu` / `_bigquery_daily_cu`; reads `warehouse_window_stats`, prefers the 7-day window, falls back to the largest available window, sums `est_cu_hours_fabric_warehouse` across all warehouses, converts to sustained CU/day. Folded into `projected_fabric_cu` so a Snowflake-only run lights up both Projected Fabric CU and Recommended SKU columns. `_cloud_for()` extended with a Snowflake branch honouring `extras["platform"]` ∈ {aws, azure, gcp} (default aws). SPA `ScopeFilter.tsx::SOURCE_LABELS` + `EstateSummaryCard.tsx::TYPE_LABEL` Snowflake entries verified present from 7-E. 9 new tests in `tests/web/test_estate_snowflake.py` (helper edge cases incl. 7-day preference, largest-window fallback, bad-number swallowing, all-zero, `_cloud_for` platform routing + AWS default, end-to-end Snowflake-only run with `extras["platform"]="azure"` asserting `projected_fabric_cu=8.0` + non-null SKU + `cloud="azure"`). *7-G:* New `docs/user-guide/23-snowflake.md` mirroring `21-databricks.md` / `22-bigquery.md` (alpha banner, scope shape, CLI quick start, OAuth setup incl. `ALTER SECURITY INTEGRATION` byte-exact `OAUTH_REDIRECT_URI='http://localhost:53682/'` step, role requirements, support matrix with `cost` ✅ post-7-H, collected shapes, Estate Overview CU rollup section, Cost section detailing the `METERING_HISTORY` + `USAGE_IN_CURRENCY_DAILY` join, limits, roadmap). New ADR-0007 (Snowflake auth — OAuth refresh-token chosen over key-pair JWT / External OAuth / PAT) and ADR-0008 (Snowflake multi-cloud via `CURRENT_REGION()` prefix sniffing). `docs/adr/README.md`, `docs/user-guide/README.md`, `README.md`, and `QUICKSTART.md` indexes / source-status blocks updated to include Snowflake. *7-H:* New `src/usma/modules/cost/snowflake_cost_client.py` running three queries — rate derivation from `USAGE_IN_CURRENCY_DAILY` (`usage_type IN ('compute','cloud services')`), per-warehouse compute from `METERING_HISTORY` × derived rate, monthly storage from `USAGE_IN_CURRENCY_DAILY` (`usage_type='storage'`). Emits `MonthlyCostRow` with `resource_kind ∈ {snowflake_warehouse, cloud_services, storage}`. Status vocabulary `ok | sdk_missing | live_disabled | missing_config | empty_window | error`, honouring the shared `SMA_COST_DISABLE_LIVE` kill-switch. Reuses `SnowflakeProvider.make_clients(...).connect()` so the OAuth-refresh-token plumbing from 7-E.1 drives cost reads with zero new auth surface. `CostAnalyzer.__init__` + `run()` branched on `scope.type == SourceType.SNOWFLAKE`; `_client.filter_rows_for_scope` skipped (rows are already account-scoped). `MODULE_SPECS["cost"].supports` widened to include `SNOWFLAKE`. `Run.tsx::MODULES_BY_SOURCE.snowflake` widened to `{snowflake_workloads, cost, fabric_mapping}`. 9 new tests in `tests/test_snowflake_slice_7h.py` (account-id required, `live_disabled` + `missing_config` short circuits, live happy-path mocking `SnowflakeProvider.make_clients` with a fake connection returning canned rows incl. CLOUD_SERVICES rollup, `empty_window`, zero-rate degradation showing usage volume but cost=0, `CostAnalyzer` dispatch routing Snowflake to `SnowflakeCostClient`, `MODULE_SPECS["cost"].supports` snowflake assertion); `tests/sources/test_module_spec.py::test_applicable_modules_filters_by_source` widened so `applicable_modules(SourceType.SNOWFLAKE) == {snowflake_workloads, cost, fabric_mapping}`. **876 → 893 Python tests passing.** SPA vitest 25/25; `tsc --noEmit` clean. |
| 2026-05-28 | 7 | **Phase 7 Slice 7-E.2 — Browser sign-in to Snowflake.** Companion to 7-E.1. New `web/api/snowflake_auth.py` (sibling of Slice 5-I `gcp_auth.py`) exposes `POST /api/auth/snowflake/login` + `GET /api/auth/snowflake/status` + `POST /api/auth/snowflake/reset` registered on `prefix="/api/auth"`. `POST /login` reads `SNOWFLAKE_ACCOUNT` / `SNOWFLAKE_OAUTH_CLIENT_ID` / `SNOWFLAKE_OAUTH_CLIENT_SECRET` from the configured `.env` (400 with the missing-key list if any absent), spawns a daemon thread that picks a free localhost port, stands up a one-shot stdlib `http.server.HTTPServer`, opens the user's default browser at `https://<account>.snowflakecomputing.com/oauth/authorize?response_type=code&client_id=…&redirect_uri=http://localhost:<port>/&scope=refresh_token[+session:role:<ROLE>]`, waits up to 5 minutes for the callback, POSTs `grant_type=authorization_code` to `/oauth/token-request` via stdlib `urllib.request`, and persists the resulting `refresh_token` to the project `.env` via `dotenv.set_key`. Access token from the exchange is *not* persisted (~10-min TTL — the analyzer mints fresh ones on demand via 7-E.1). Single-flight semantics + 409 on `reset` while pending + every failure path recorded into `_status.error` so the UI never gets stuck on `pending`. SPA: `loader.ts` gains `apiSnowflakeAuthLogin` / `apiSnowflakeAuthStatus` / `apiSnowflakeAuthReset` + matching `SnowflakeAuthState` / `SnowflakeAuthStatus` types. `Configuration.tsx` Snowflake form-block grows a **Sign in to Snowflake** button + status pill + localhost-only caveat banner directly under the OAuth client id/secret rows (gated on account + client_id present and client_secret = `set`); while pending the SPA polls every 1500ms; on `ready` it reloads `apiGetConfig()` so the refresh-token pill flips to `set` and clears the cached `workspaces` so the existing auto-discover effect re-fires. 13 new tests in `tests/test_snowflake_slice_7e2.py`. **863 → 876 Python tests passing.** SPA vitest 25/25, `tsc --noEmit` clean. |
| 2026-05-28 | 7 | **Phase 7 Slice 7-E.1 — Snowflake OAuth rewire.** Replaces Slice 7-B's key-pair JWT auth with Snowflake's built-in OAuth (refresh-token grant — `client_credentials` is unsupported on built-in OAuth). New env vars `SNOWFLAKE_OAUTH_CLIENT_ID` / `SNOWFLAKE_OAUTH_CLIENT_SECRET` / `SNOWFLAKE_OAUTH_REFRESH_TOKEN` drive a runtime token mint at `https://<account>.snowflakecomputing.com/oauth/token-request` via stdlib `urllib.request` (no `requests` dep added — mirrors `modules/cost/fabric_pricing.py`); optional `SNOWFLAKE_OAUTH_TOKEN` pre-minted access-token escape hatch for CI / break-glass. Removed env vars `SNOWFLAKE_PRIVATE_KEY` / `SNOWFLAKE_PRIVATE_KEY_PASSPHRASE`. `provider.py`: `required_env` widened to the 6-tuple; new `_exchange_refresh_token()` POSTs form-urlencoded `grant_type=refresh_token` and returns `access_token`; `_resolve_connect_kwargs()` returns connector kwargs with `authenticator='oauth'` + `token=<access>`; `make_clients()` re-resolves on every `connect()` so access tokens stay fresh under the ~10-min Snowflake OAuth TTL; `_load_private_key()` + `cryptography.hazmat` import dropped. `schemas.py`: `AzureConfigPublic` drops `snowflake_private_key` / `_passphrase`, adds `snowflake_oauth_client_id` plain + `snowflake_oauth_client_secret` / `_refresh_token` / `_token` collapsed to `set`/`unset`; `AzureConfigUpdate` accepts the three secrets write-only. `config_io.py`: `_SNOWFLAKE_PLAIN_KEYS` / `_SNOWFLAKE_SECRET_KEYS` swapped; `discover_snowflake_databases` docstring + `validate_config_live` Snowflake check renamed to "Snowflake OAuth token resolution". `pyproject.toml`: `[snowflake]` + `[dev]` extras drop the explicit `cryptography>=42.0` pin (still pulled transitively by the connector). SPA: `loader.ts` `AppConfig.azure` shape updated; `Configuration.tsx` replaces the PEM textarea + passphrase input with an OAuth client id text input + 3 password/textarea inputs (client secret + refresh token + optional pre-minted access token) and an updated explanatory paragraph; `credsReady` gate requires either `snowflake_oauth_token === "set"` OR (`client_id` + `client_secret` + `refresh_token` all populated). Test files rewritten across `tests/sources/snowflake/test_provider.py` (registry hygiene → OAuth required-env tuple) + `tests/sources/snowflake/test_provider_slice_7b.py` (~30 tests including new `_exchange_refresh_token` happy / HTTP-error / missing-access_token paths + pre-minted-token-skips-exchange + token-minted-fresh-on-every-connect) + `tests/test_snowflake_slice_7e.py` (seed helper + persist/read/clear assertions swapped to OAuth env vars). **858 → 863 Python tests passing.** SPA vitest 25/25, `tsc --noEmit` clean. ADR-0007 to land alongside Slice 7-G docs. |
| 2026-05-28 | 7 | **Phase 7 Slice 7-E — SPA Configuration + Run page wiring for Snowflake.** `web/schemas.py` widens `ScopeRef`/`AzureConfigPublic`/`AzureConfigUpdate` `source_type` Literals to include `"snowflake"` and adds `snowflake_account`/`snowflake_user`/`snowflake_role`/`snowflake_warehouse`/`snowflake_platform` plain fields plus write-only `snowflake_private_key`/`snowflake_private_key_passphrase` secrets (collapsed to `set`/`unset` on reads). `web/config_io.py` extends `read_config`/`write_config` to round-trip `SNOWFLAKE_*` env vars + `SMA_SNOWFLAKE_PLATFORM`, adds `discover_snowflake_databases()` (single-account `WorkspaceSummary` via `SnowflakeProvider.discover` — no AAD required), and adds a Snowflake branch in `validate_config_live` that bypasses both the `_REQUIRED_LIVE` AAD gate and the Synapse SQL gauntlet (mirrors the BigQuery branch). New `POST /api/config/discover-snowflake-databases` route. SPA: fifth radio in `Configuration.tsx` (alpha badge) drives a new `bucket="snowflake"` form (account/user/role/warehouse text fields + PEM textarea + passphrase password input, write-only contract mirrors `databricks_client_secret`); `Run.tsx` `MODULES_BY_SOURCE.snowflake = {snowflake_workloads, fabric_mapping}` (cost added in 7-H); `ALL_MODULES` widens; new `apiDiscoverSnowflakeDatabases()` loader; `ScopeFilter.tsx`/`lib/labels.ts` pick up Snowflake entries. 9 new tests in `tests/test_snowflake_slice_7e.py`. **849 → 858 Python tests passing.** SPA vitest 25/25, `tsc --noEmit` clean. |
| 2026-05-28 | 7 | **Phase 7 Slice 7-D — Credit → vCore-hour → CU rollup.** New `modules/snowflake_workloads/run_stats.py` ships the warehouse-size proxy (X-SMALL=1 → 6X-LARGE=512 doubling progression for both credits-per-hour and vCore-per-credit), `CREDIT_TO_VCORE_HOURS=1.0` + `CREDIT_TO_CU_HOURS=0.5` constants derived from the shared `VCORE_HOURS_TO_CU_HOURS=0.5` so a single edit rebalances all four clouds, `populate_warehouse_size_metrics` + `populate_job_credits` (prefers `execution_ms` over `total_elapsed_ms`), `aggregate_jobs_by_window` (one row per 7/14/28/90-day window), `aggregate_warehouse_metering_by_window` (per warehouse per window, sorted, unattributable jobs skipped), and `CREDIT_TO_CU_CAVEAT` calling out the proxy + the planned `SMA_SNOWFLAKE_CREDIT_TO_VCORE_RATIO` calibration knob. `SnowflakeWorkloadsAnalyzer.run()` now applies the warehouse + job populators, runs both aggregators, and appends the caveat exactly once when any math was applied. New `fabric_mapping/rules.py::rules_for_snowflake_workloads` emits inventory + per-kind unsupported/partial table rollups + routine kind×language coverage with JS / Java flagged as unsupported + Snowflake task / stream / pipe / stage replatform narratives + warehouse F-SKU rec via `_smallest_sku_covering` from the 7-day window + credit-caveat passthrough. `MODULE_SPECS["fabric_mapping"].supports` widened to include `SNOWFLAKE`; `fabric_mapping/analyzer.py` registers `snowflake_workloads.json` + the new rule. 29 new tests in `tests/modules/snowflake_workloads/test_run_stats.py` (constants doubling progression, populate exec-vs-elapsed preference + unknown-size no-ops, window aggregation edge cases, end-to-end analyzer wiring with caveat-appended-once assertion) and `test_rules.py` (inventory, table rollups, routine flagging, F-SKU math `336 CU-hr / 7 days → F2`, caveat passthrough); `test_module_spec.py` updated so `applicable_modules(SourceType.SNOWFLAKE) == {snowflake_workloads, fabric_mapping}`. Snowflake cost (METERING_HISTORY + USAGE_IN_CURRENCY_DAILY) deferred to 7-H per scope split. **820 → 849 Python tests passing.** |
| 2026-05-28 | 7 | **Phase 7 Slice 7-C — `snowflake_workloads` module.** New `modules/snowflake_workloads/` package with `models.py` (Warehouse, Database, Schema, Table, Routine, Stage, Stream, Task, Pipe, SnowflakeJob + placeholder window-stats shapes + `SnowflakeWorkloadsAnalysis` carrying `account`/`platform`/`region`/`edition`), `fabric_compat.py` (per-kind table verdicts, FUNCTION/PROCEDURE × SQL/JS/Python/Java/Scala routine matrix with JS+Java unsupported, STAGE/STREAM/TASK/PIPE object verdicts), `collector.py` (`SnowflakeWorkloadsCollector` wrapping the `SnowflakeClientBundle.connect` callable; SHOW-based inventory + `ACCOUNT_USAGE.QUERY_HISTORY` job pull with `_normalise_size` / `_outcome` helpers), `analyzer.py` (`for_descriptor()` factory, env-driven lookback/limit/system-DB toggles, per-schema error isolation, support-label roll-ups), and `reporting.py` (JSON + 12 CSVs + markdown narrative). `MODULE_REGISTRY` + `MODULE_SPECS` widened so `applicable_modules(SourceType.SNOWFLAKE) == {snowflake_workloads}`. 24 new tests in `tests/modules/snowflake_workloads/test_analyzer.py` covering classification tables, collector SHOW-parsing for every object family, analyzer orchestration (system-DB skip, `INFORMATION_SCHEMA` filter, per-schema error isolation), MODULE_REGISTRY wiring, and report writers; existing `test_module_spec.py` expectations updated. Credit → vCore-hour → CU math, `rules_for_snowflake_workloads`, and warehouse F-SKU mapping deferred to 7-D. **796 → 820 Python tests passing.** |
| 2026-05-28 | 7 | **Phase 7 Slice 7-B — `SnowflakeProvider` real implementation.** `sources/snowflake/provider.py`: `discover()` returns a single descriptor for the configured account (Snowflake scope-unit is the account, established by the key-pair JWT auth contract); `validate()` runs key-pair JWT resolution + `CURRENT_VERSION()` + `ACCOUNT_USAGE.WAREHOUSE_METERING_HISTORY` reachability probes; `make_clients()` returns a `SnowflakeClientBundle` with a lazy zero-arg `connect()`. `extras["platform"]` resolves via `CURRENT_REGION()` prefix (`AWS_*` → aws, `AZURE_*` → azure, `GCP_*` → gcp) with `SMA_SNOWFLAKE_PLATFORM` env fallback and final `"aws"` default. Auth via `Credentials.extras` (`snowflake_account`/`snowflake_user`/`snowflake_private_key`/`snowflake_private_key_passphrase`/`snowflake_role`/`snowflake_warehouse`) with `SNOWFLAKE_*` env fallback; private key accepts inline PEM or a path, loaded via `cryptography.hazmat` and re-serialised to PKCS8 DER. CLI: new `--scope snowflake:<account>[@<platform>]` shortcut mirroring `databricks-aws:<host>`. New `host_to_descriptor()` helper. 21 new tests (16 provider unit tests + 5 CLI scope-flag tests). **775 → 796 Python tests passing.** No SPA wiring yet (Slice 7-E). |
| 2026-05-28 | 7 | **Phase 7 scoped — Snowflake as fourth cloud source.** Feasibility review against the Databricks integration surface (provider, collector, fabric compat, cost-skip, CLI scope shortcut, SPA Configuration radio + auth panel, env-var family, ADR, user-guide, ~21 test files) confirmed the plugin seams hold cleanly: `SourceProvider`, `SourceDescriptor` + `extras`, `MODULE_REGISTRY`, `fabric_mapping/rules.py` rule-per-artifact, and the Estate Overview cloud bucketing (just patched in 5.1.3 for AWS Databricks) all accept Snowflake without architectural changes. Estimated ~60% copy-paste-rename / 40% targeted edits; no prerequisite refactor. Locked 10 slices 7-A → 7-J mirroring the BigQuery (Phase 5) shape: pre-flight enum / extras / provider stub (7-A), `SnowflakeProvider` with key-pair JWT + multi-cloud `CURRENT_REGION()` sniffing (7-B), `snowflake_workloads` module + compat matrix sign-off gate (7-C), credit → vCore-hour → CU rollup with warehouse-size → vCPU proxy (7-D), SPA Configuration + Run page widening (7-E), Estate Overview CU rollup (7-F), docs + ADR-0007 auth decision (7-G), Snowflake cost via `SNOWFLAKE.ACCOUNT_USAGE.METERING_HISTORY` + `USAGE_IN_CURRENCY_DAILY` (7-H), Dashboard section (7-I), multi-cloud parity smoke (7-J). Snowpark container services, Data Sharing / Cortex, Iceberg-on-external-storage, and dbt artifacts explicitly out-of-scope for Phase 7. Default auth: key-pair JWT primary, External OAuth via Entra documented as follow-up. Estimated ~3–4 focused weeks for full Databricks parity. |
| 2026-05-22 | 5 | **Phase 5 Slice 5-J — BigQuery Dashboard section (closes Phase 5 UI)**. Commits `3259355` (collection + per-day / per-dimension / table-usage aggregation), `35adcbe` (initial standalone `/bigquery` SPA route — superseded), `9def906` (sqlglot BigQuery-dialect query-feature extraction, gated on `SMA_BQ_CAPTURE_QUERY_TEXT=1`), `f972722` (folded BigQuery into the Dashboard and removed the standalone route + nav tab), `b646509` (last-24h hourly bar chart stacked per `statement_type`, prefers `total_slot_hours` with run-count fallback — mirrors `DatabricksHourlyBars` palette + dashed average line + layout), `04f184c` (daily + hourly charts laid out side-by-side via the same `flex 1 1 360px` row used by the Databricks pair), `b96e717` (GCS storage view: rolls up per-table `num_bytes` into per-dataset logical-storage with a 3-card headline + per-dataset table + largest-table column, explicit note that the metric is BigQuery's active logical bytes and that physical / long-term split would require `INFORMATION_SCHEMA.TABLE_STORAGE`). End result: BigQuery lives inside `Dashboard.tsx` as `BigQueryWorkloadsSection` between `DatabricksWorkflowsSection` and `ServerlessSection`, byte-faithful to the existing Synapse / Databricks / Spark / Serverless idiom (`<section className="section">`, `ProvenanceBadge` + `ReanalyzeButton`, `cols-3` `StatCard` headline, fixed-geometry inline SVG charts at 720×240/padX=56, plain table chrome with `.num` right-aligned cells, `var(--ok)` / `var(--err)` / `var(--muted)` colors). `pages/BigQuery.tsx` deleted; `App.tsx` `STATIC_NAV` + `CONTROL_PLANE_NAV` + `<Route path="/bigquery">` all pruned. Build clean, 25 vitest tests passing, 73 Python tests passing across the BigQuery slice files. Phase 5 UI exit criterion met. |
| 2026-05-21 | 5 | **Phase 5 Slice 5-I — browser sign-in to GCP from the SPA (no gcloud CLI required)**. New `web/api/gcp_auth.py` exposes `POST /api/auth/gcp/login` + `GET /api/auth/gcp/status` + `POST /api/auth/gcp/reset` registered on `prefix="/api/auth"` in `web/app.py`. `POST /login` is non-blocking: takes a `threading.Lock`, flips the module-level `_Status` to `state="pending"`, then spawns a daemon thread running `google_auth_oauthlib.flow.InstalledAppFlow.from_client_config({...installed: gcloud's public OAuth client_id / client_secret...}, scopes=[cloud-platform, sqlservice.login, openid, userinfo.email]).run_local_server(port=0, open_browser=True, prompt="consent", access_type="offline")`. The browser callback hits the one-shot localhost listener; on success the refresh token is persisted as a gcloud-compatible `authorized_user` JSON at the canonical ADC path (`%APPDATA%\gcloud\application_default_credentials.json` on Windows; `~/.config/gcloud/...` on POSIX) so the next `google.auth.default()` call inside `BigQueryProvider` / `GcpCostClient` finds it without any env var. ID token is base64-decoded best-effort to surface the user's email. State machine is `idle → pending → ready | error`; second `POST /login` while pending is a no-op (returns `pending` + "already in progress" message) to keep single-flight semantics around the localhost port. `POST /reset` clears `ready`/`error` state but returns 409 while `pending` (the background thread still owns the listener). All failure paths (missing `google-auth-oauthlib` install, user denied consent, Google returned no refresh token, ADC file write failed) record `state=error` with a human-readable message — the thread never dies silently leaving the UI stuck on `pending`. SPA: `loader.ts` gains `apiGcpAuthLogin()` / `apiGcpAuthStatus()` / `apiGcpAuthReset()` + matching `GcpAuthState` / `GcpAuthStatus` types. `Configuration.tsx` adds a **Sign in to Google** button + status pill + caveat banner ("only works when SMA runs on your local machine — the OAuth callback redirects to localhost") inside the existing BigQuery card; while `gcpAuth.state === "pending"` the SPA polls `/api/auth/gcp/status` every 1500ms; on `ready` it clears the cached `workspaces` so the existing auto-discover effect re-fires against the new ADC token and the project dropdown populates without a second click. `pyproject.toml`: added `google-auth-oauthlib>=1.2` to both the `[bigquery]` optional extra and the `[dev]` rollup (not transitively pulled by `google-cloud-bigquery`, verified by `pip install google-auth-oauthlib`). `docs/user-guide/22-bigquery.md` Authentication section gains a third numbered option flagged as preferred for dev boxes; existing SA-key + `gcloud login` options retained for CI / server deployments. 9 new tests in `tests/test_bigquery_slice_5i.py` (status starts idle; success path writes ADC JSON with `type=authorized_user` + extracts email from ID token + passes `access_type=offline` / `prompt=consent` to `run_local_server`; concurrent POST returns `pending` without starting a second thread, verified with a `threading.Event` gate; flow exception records `state=error` with the exception message; refresh-token absence is rejected as error; missing `google-auth-oauthlib` import records a setup-error message pointing at the `[bigquery]` extra; reset after `ready` clears state; reset while `pending` returns 409; CSRF guard — POST /login without `X-SMA-API: 1` header returns 400 from the global middleware). All 9 pass in 5.4s. **633 Python tests passing** (624 prior + 9 new; pre-existing `test_estate_index_caches_and_invalidates_on_mtime` Windows NTFS mtime-resolution flake unchanged — passes in isolation, occasionally fails in the full-suite run, unrelated to this slice and reproducible without the 5-I changes), **25 vitest tests passing**, SPA build clean. Customers on org-policy `iam.disableServiceAccountKeyCreation` who refuse to install gcloud CLI can now produce ADC credentials with one button click. |
| 2026-05-21 | 5 | **Phase 5 Slice 5-H — GCP cost client + `cost.supports` widened to BIGQUERY** (closes Phase 5). New `modules/cost/gcp_cost_client.py` mirrors the Azure `cost_client.py` interface (`fetch_monthly_breakdown_with_status(start, end) -> (rows, status)`) so the existing aggregation / rules / Fabric-comparison machinery in `CostAnalyzer` is reused unchanged. The collector queries the project's BigQuery billing-export table (`gcp_billing_export_v1_<account-id>` per Google's naming) via the `google.cloud.bigquery` SDK; SQL groups by `(month, service.description, sku.description, project.id, currency)` with `SUM(cost)` + `SUM(usage.amount)`. Configuration is env-var driven: `SMA_GCP_BILLING_DATASET` (fully-qualified `host-project.dataset` or bare `dataset` — auto-qualified with the scope project id), plus `SMA_GCP_BILLING_TABLE` **or** `SMA_GCP_BILLING_ACCOUNT` (derives the table name as `gcp_billing_export_v1_<ACCOUNT>` with hyphens → underscores per Google's naming). Honors the same `SMA_COST_DISABLE_LIVE` kill-switch as the Azure client. Status vocabulary mirrors Azure's: `ok` | `sdk_missing` | `live_disabled` | `missing_config` | `empty_window` | `error` — missing-config is a soft-skip (logs an info line, returns empty rows) so a BigQuery scope without billing export configured still finishes the analyzer with `collection_status="missing_config"` instead of an error. New `_classify_service(service)` maps GCP `service.description` strings to normalised `resource_kind` buckets (`bigquery_project`, `dataflow_job`, `dataproc_cluster`, `storage`, `compute`, `other`). `CostAnalyzer.__init__` now dispatches on `cfg.primary_scope().type`: BigQuery scopes get `GcpCostClient(scope)`, all others retain the existing `CostClient(cfg.azure)`. `CostAnalyzer.run()` also branches the `CostAnalysis` header fields: BigQuery uses `scope.id` (project id) for both `workspace_name` and `subscription_id`, and an empty string for `resource_group` (no ARM concept on GCP). `MODULE_SPECS["cost"].supports` widened to `{SYNAPSE_WORKSPACE, ADF, DATABRICKS, BIGQUERY}`. `config.py`: `AzureConfig` gains optional `gcp_project_id: str \| None`; `load_config()` adds a `SMA_SOURCE_TYPE=bigquery` branch that **relaxes** the `_REQUIRED` AAD env-var gate (ADC handles auth — Azure fields default to empty strings) and validates that `SMA_GCP_PROJECT_ID` is present. New `_legacy_bigquery_scope(azure)` builds a `SourceDescriptor(type=BIGQUERY, id=project_id, display_name=project_id)` so the wave runner can be driven by a BigQuery-only `.env`. SPA: `MODULES_BY_SOURCE.bigquery` flipped from `{bigquery_workloads, fabric_mapping}` to `{bigquery_workloads, cost, fabric_mapping}`; `Configuration.tsx` BigQuery guidance paragraph rewritten to reflect cost now runs and to call out the three billing-export env vars. `docs/user-guide/22-bigquery.md` flipped: support-matrix `cost` row ⛔ → ✅; Run-page section enumerates `cost` as a default-on module with the env-var contract; Limits & known gaps replaces the "No GCP cost attribution yet" bullet with a "GCP cost attribution requires a billing export to BigQuery" bullet documenting the soft-skip on `missing_config`; Roadmap marks 5-H as shipped. 22 new tests in `tests/test_bigquery_slice_5h.py` (1 spec-widening; 3 `_resolve_billing_table` paths — missing, unqualified dataset, account-derived; 9 parametrized `_classify_service` cases; 2 `_parse_billing_rows` tests including sku-blank fallback; 3 short-circuit tests — `live_disabled`, `missing_config`, empty project id; 2 live-path tests against a fake `google.cloud.bigquery` injected via `sys.modules` patching — happy-path SQL/params/project capture + empty-result `empty_window` status; 2 `CostAnalyzer` dispatch tests — BigQuery scope routes to `GcpCostClient` with correct `workspace_name`/`subscription_id`/`resource_group`, Azure scope still routes to `CostClient`); `tests/sources/test_module_spec.py` updated so `applicable_modules(BIGQUERY) == {bigquery_workloads, cost, fabric_mapping}`. **624 Python tests passing** (602 prior + 22 new), **25 vitest tests passing**, SPA build clean. Phase 5 closes. |
| 2026-05-21 | 5 | **Phase 5 Slice 5-F — BigQuery CU rollup in the Estate Overview**. New `_bigquery_daily_cu(bigquery)` helper in `web/estate.py` (sibling of `_databricks_daily_cu`). Unlike `databricks_workflows.json`'s per-workflow `windows` array, `bigquery_workloads.json` carries a single flat `job_window_stats` list (one row per rolling window: 7 / 14 / 28 / 90 days). The helper prefers the 7-day row (matches the Dashboard tile selection); falls back to the largest available window when 7-day is absent; reads `est_cu_hours_fabric_spark` (slot-hour → CU-hour conversion already happened inside the BigQuery analyzer via `SLOT_HOURS_TO_CU_HOURS = 0.25`); converts the window total to sustained CU/day (`cu_hr / window_days / 24`); returns `0.0` on absent file, missing `job_window_stats`, non-numeric CU value, or `window_days <= 0`. `_extract_run` now reads `bigquery_workloads.json` alongside `databricks_workflows.json`, computes `bigquery_daily_cu`, and folds it into `projected_fabric_cu` with the same null-coalescing guard (`None` only when DW + pipelines + Databricks + BigQuery are all zero/absent). The existing `_smallest_sku_covering` lazy import keeps deriving a `recommended_fabric_sku` from the combined CU when `capacity_projection.recommended_sku` is null — so a BigQuery-only run with no DW capacity projection now lights up both columns in the Estate Overview. 7 new tests in `tests/web/test_estate_bigquery.py`: 6 direct unit tests of `_bigquery_daily_cu` (empty payload, missing/non-list `job_window_stats`, 7-day preference exact math, largest-window fallback exact math, non-numeric CU swallowed, `window_days=0` / missing `window_days` rejected) and one end-to-end test that builds a BigQuery-only run dir (no `capacity_projection.estimated_cu`, `scopes=[{source_type=bigquery}]`, 1344 CU-hr / 7 days = 8.0 sustained CU/day) and asserts `EstateIndex.build()` produces a single workspace with `projected_fabric_cu == 8.0`, a non-null `recommended_fabric_sku` starting with `F`, and `source_type == "bigquery"`. **602 Python tests passing** (594 prior + 7 new + the previously-flaky `test_estate_index_caches_and_invalidates_on_mtime` happened to pass this run — the NTFS mtime resolution flake remains a known issue unrelated to this slice). |
| 2026-05-21 | 5 | **Phase 5 Slice 5-E — BigQuery docs**. New `docs/user-guide/22-bigquery.md` mirrors `21-databricks.md`: alpha banner, `SourceDescriptor` shape (note `subscription_id=None` / `resource_group=None` — GCP project id is the scope key, no ARM URL), CLI quick start with the `bigquery:<project-id>` scope form (no `@<sub>/<rg>` suffix), authentication section covering both ADC paths (service-account JSON key via `GOOGLE_APPLICATION_CREDENTIALS` for CI; `gcloud auth application-default login` for dev), explicit IAM-role list (Metadata Viewer, Data Viewer, Logs Viewer, optional DTS User, optional org-level Browser for project discovery), support matrix widened with BigQuery column (`bigquery_workloads` only here; `cost` explicitly marked ⛔ with the 5-H link), collected-shapes section enumerating `iter_datasets` / `iter_tables` / `iter_routines` / `iter_scheduled_queries` / `iter_jobs(lookback_days)`, a dedicated subsection documenting the `SLOT_HOURS_TO_VCORE_HOURS = 0.5` / `SLOT_HOURS_TO_CU_HOURS = 0.25` constants and rolling-window scheme from 5-C, Configuration / Run page tour (calls out ADC-only auth — no AAD fields required, dual persistence of `SMA_GCP_PROJECT_ID` + `SYNAPSE_WORKSPACE_NAME` for SPA display-name continuity, validate skipping the Synapse SQL gauntlet), limits section (no GCP cost yet → 5-H, slot ≈ 0.5 vCore is an assumption, no INFORMATION_SCHEMA.JOBS fallback, no estate-overview rollup yet → 5-F, no BigLake/Omni/ML deep-dive), and a roadmap pointing at 5-F / 5-H / Phase 6+. Threaded into supporting docs: `docs/user-guide/README.md` (chapter 22 added to the index), `README.md` (lead paragraph mentions BigQuery; supported-sources block gains a 🟡 BigQuery alpha row pointing at chapter 22; modules block gains a `bigquery_workloads` bullet describing the five collected shapes + the slot-hour rollup with caveat), `QUICKSTART.md` (opening paragraph mentions BigQuery v0.8 alpha; sources callout points readers at chapter 22 + flags ADC instead of SP; module table gains row 4c for `bigquery_workloads`), `docs/user-guide/01-getting-started.md` Mermaid ("Pick source type" widened to 4 entries, auto-discover bubble adds GCP projects + ADC, RBAC fallback bubble mentions "Browser role for GCP", per-source-role hint adds the BigQuery IAM block, Run-page module list adds `bigquery_workloads` + flags that `cost` is currently Synapse + ADF + Databricks only). No code changes — docs-only slice. **594 Python tests / 25 vitest tests unchanged from 5-D baseline.** |
| 2026-05-21 | 5 | **Slice 5-D follow-up** (commit `826dbef`). `quickstart.ps1` ran `npm run build` which surfaced two TS2741 errors hidden by the vitest-only suite: `web/src/components/EstateSummaryCard.tsx::TYPE_LABEL` (a `Record<SourceTypeId, string>` map) and `web/src/components/ScopeFilter.tsx::SOURCE_LABELS` (a `Record<ScopeRef["source_type"], string>` map) both held local label dictionaries that were not updated when 5-D widened the underlying unions. Added `bigquery: "BigQuery"` to both records. `npm run build` clean. Process note: when widening `SourceTypeId` or `ScopeRef["source_type"]`, also run `npm run build` (not just `vitest`) or grep `Record<SourceTypeId` / `Record<ScopeRef\[` before committing. |
| 2026-05-21 | 5 | **Phase 5 Slice 5-D — BigQuery SPA + backend wiring**. Backend: `web/schemas.py` widens `ScopeRef.source_type`, `AzureConfigPublic.source_type`, and `AzureConfigUpdate.source_type` Literals to include `"bigquery"`; both config models gain a `gcp_project_id: str \| None = None` field. `web/config_io.py` introduces `_GCP_PROJECT_ID_KEY = "SMA_GCP_PROJECT_ID"`, persists it on `write_config` (empty → `unset_key`), surfaces it on `read_config`, and ships `discover_bigquery_projects(env_file)` — ADC-only, lazy-loads `BigQueryProvider`, calls `provider.discover(None)`, maps each `SourceDescriptor` → `WorkspaceSummary(name=display_name, resource_group=project_number, is_current=(d.id == SMA_GCP_PROJECT_ID))`, swallows provider errors into a failing `ConfigCheck`. `validate_config_live` learns a BigQuery branch placed **before** the `_REQUIRED_LIVE` gate so it skips the Azure AAD env-var check entirely (ADC handles auth); routes through `provider.validate(descriptor, None)` and best-effort `provider.discover(None)`. New `POST /api/config/discover-bigquery-projects` route in `web/api/config.py`. SPA: `sourceTypes.ts`, `loader.ts` `SourceType` union, and `loader.ts` `ScopeRef.source_type` all widened to include `"bigquery"`; `AppConfig.azure` gains `gcp_project_id: string \| null`; new `apiDiscoverBigqueryProjects()` client helper. `Run.tsx` adds `"bigquery_workloads"` to `ALL_MODULES`, registers `MODULES_BY_SOURCE.bigquery = {bigquery_workloads, fabric_mapping}` (**diverges from the manifest text** — cost not yet widened to BIGQUERY; tracked in Slice 5-H), filters it out of `synapse_workspace`'s module set, and renders a per-source guidance paragraph. `labels.ts` gains `bigquery_workloads` + `databricks_workflows` labels. `Configuration.tsx` adds a fourth radio (Synapse / ADF / Databricks / BigQuery), extends `scopeNoun` / `scopeNounPlural` / `scopeLabel` / `discoverVerbObject` / `discoverFor` for BigQuery, overrides `credsReady` to be `true` for BigQuery (ADC-only — no tenant/client/secret/subscription required), wires the auto-discover effect to fire on BigQuery without Azure creds, adds a dedicated GCP-project selector card that persists both `gcp_project_id` and `workspace_name` (mirror so existing scope-chip code keeps rendering), and clears `gcp_project_id` on source-type toggle. 8 new backend tests in `tests/test_bigquery_slice_5d.py` (read/write round-trip, `discover_bigquery_projects` happy-path + error-swallow + no-AZURE-env path, `validate_config_live` BigQuery branch routes through provider + skips Synapse probes + short-circuits on missing project, `/api/config/discover-bigquery-projects` route end-to-end via TestClient). **594 Python tests passing** (1 pre-existing `test_estate.py` mtime-cache flake unrelated to this slice), **25 vitest tests passing**, SPA build clean. |
| 2026-05-21 | 5 | **Phase 5 Slice 5-C — slot-hour → CU-hour + Fabric mapping rule for BigQuery** (commit `04523b8`). New `modules/bigquery_workloads/run_stats.py` mirrors `databricks_workflows/run_stats.py`: module-level conversion constants `SLOT_HOURS_TO_VCORE_HOURS = 0.5` (BigQuery slot ≈ 0.5 vCore, the conservative on-demand-pricing default) and `SLOT_HOURS_TO_CU_HOURS = 0.25` (= 0.5 × `VCORE_HOURS_TO_CU_HOURS` reused from the Spark-pools client so one edit changes all three sources); helpers `slot_hours_for_ms` / `cu_hours_for_slot_hours` / `populate_slot_hours(job)`; rolling-window aggregator `aggregate_jobs_by_window` over default windows (7, 14, 28, 90) producing `BigQueryJobWindowStats` rows with `total_slot_hours` + `est_cu_hours_fabric_spark` + `success_rate` + `avg_duration_seconds` + `total_billed_bytes`; standardised caveat string `SLOT_TO_CU_CAVEAT` that the analyzer appends to `BigQueryWorkloadsAnalysis.caveats` whenever any slot-hour math was actually applied (only once per run, idempotent). `BigQueryWorkloadsAnalyzer.run()` now populates per-job `slot_hours` + `est_cu_hours_fabric_spark` after the audit-log collection step and computes `job_window_stats`. New `rules_for_bigquery_workloads(payload, *, source_type=None)` in `fabric_mapping/rules.py` mirrors `rules_for_databricks_workflows`: emits `bq.inventory`, per-table-kind unsupported/partial rollups (SNAPSHOT → unsupported, VIEW/EXTERNAL/MATERIALIZED_VIEW → partial), routine kind summary, scheduled-query mapping note, per-job-type bucket recs (QUERY/LOAD/EXTRACT/COPY) covering audit-log observations, a `bq.failed_jobs` warning when the completed-job failure rate ≥ 10%, and a `bq.slot_hour_caveat` rec that bubbles up the analyzer's slot-ratio caveat into the recommendations stream. Registered in `fabric_mapping/analyzer.py::_INPUT_FILES["bigquery_workloads"]` + `_RULES["bigquery_workloads"]`. `MODULE_SPECS["fabric_mapping"].supports` widened to include `BIGQUERY` (BigQuery scopes now get the inventory → Fabric translation pass). `MODULE_SPECS["cost"].supports` **not** widened — deferred to Slice 5-H once `modules/cost/gcp_cost_client.py` ships, because the current cost module hard-codes Azure Cost Management and would silently call against a GCP scope. 18 new tests: 8 in `tests/modules/bigquery_workloads/test_run_stats.py` (constant invariants, conversion helpers, populate-noop on missing `total_slot_ms`, multi-window aggregation across all four default windows, empty-input zero-fill, caveat-string content) and 10 in `tests/modules/bigquery_workloads/test_rules.py` (inventory presence, table-kind rollups, routine summary, scheduled-query rec, job-bucket recs only for buckets actually present, 40% failure-rate triggers warning while 5% does not, slot caveat bubble-up, `source_type=` kwarg accepted for API symmetry, empty payload yields no recs); `tests/sources/test_module_spec.py` updated to expect `applicable_modules(BIGQUERY) == {bigquery_workloads, fabric_mapping}`. **587 Python tests passing.** |
| 2026-05-21 | 5 | **Phase 5 Slice 5-B — `bigquery_workloads` module** (commit `f0dd508`). New analyzer module sibling of `databricks_workflows` (per ADR D5). `modules/bigquery_workloads/` ships: `models.py` (Pydantic v2: `Dataset`, `Table`, `Routine`, `ScheduledQuery`, `BigQueryJob`, `BigQueryJobWindowStats`, `BigQueryWorkloadsAnalysis` — `BigQueryJob` carries `total_slot_ms` + placeholder `slot_hours` / `est_cu_hours_fabric_spark` fields so Slice 5-C fills them in-place without a schema bump); `fabric_compat.py` (coarse `classify_table` / `classify_routine` / `job_type_bucket` lookups — Slice 5-C will widen into the full `fabric_mapping` rule); `collector.py` (`BigQueryWorkloadsCollector` wrapping duck-typed `bigquery.Client` + `logging_v2.Client` + optional `bigquery_datatransfer_v1.DataTransferServiceClient`; `iter_datasets` / `iter_tables(ds)` / `iter_routines(ds)` / `iter_scheduled_queries` / `iter_jobs(lookback_days)`; the audit-log filter `AUDIT_LOG_FILTER_TEMPLATE` is exposed for reuse and the `_extract_job_from_entry` helper handles both v2 `protoPayload.metadata.jobChange` and legacy v1 `protoPayload.serviceData.jobCompletedEvent` shapes — pattern reference: `constantino-casado/GCP_logs` audit-log field flattening); `analyzer.py` (`BigQueryWorkloadsAnalyzer` + `for_descriptor()` that builds clients from `BigQueryProvider.make_clients()`; rejects non-BigQuery descriptors; surfaces a caveat when the DTS client is unavailable rather than failing the run); `reporting.py` (JSON / CSV per row type / Markdown). Registered in `MODULE_REGISTRY["bigquery_workloads"]` via a `_bigquery_workloads` factory that requires the primary scope to be `SourceType.BIGQUERY`. Registered in `MODULE_SPECS["bigquery_workloads"]` with `supports=frozenset({BIGQUERY})` (5-C will widen `cost` + `fabric_mapping` to also support BIGQUERY). `pyproject.toml`: added `google-cloud-bigquery-datatransfer>=3.15` to both `[bigquery]` and `[dev]` so scheduled-query enumeration works out of the box on dev installs. 23 new tests in `tests/modules/bigquery_workloads/test_analyzer.py` covering fabric_compat lookups, both v2 + v1 audit-log shapes, noise-entry filtering, collector mocked end-to-end (datasets / tables / routines / jobs / scheduled queries — including the `data_source_id != "scheduled_query"` filter), analyzer happy-path + DTS-missing caveat + non-BigQuery descriptor rejection + missing-collector guard, reporting JSON-only + all-formats, and registry/spec integration; `tests/sources/test_module_spec.py` extended for the BIGQUERY `applicable_modules` check. **569 Python tests passing.** |

| 2026-05-20 | 5 | **Phase 5 Slice 5-A — `BigQueryProvider`** (commit `cd06e81`). Real implementation replaces the Phase-0 plan-only entry. `sources/bigquery/provider.py` ships `BigQueryProvider` (registered in `SOURCE_REGISTRY[BIGQUERY]` via the `sources/__init__.py` import chain) with `discover()` enumerating active GCP projects via `google.cloud.resourcemanager_v3.ProjectsClient.search_projects(query="state:ACTIVE")`, `validate()` running three independent probes (ADC resolution via `google.auth.default()`, then `bigquery.Client.get_service_account_email()`, then `logging_v2.Client.list_entries(filter_=..., page_size=1, max_results=1)` against the data-access audit log), and `make_clients()` returning a `BigQueryClientBundle` dataclass that mirrors `DatabricksClientBundle`. ADC-only auth — the Azure-shaped `Credentials` object is ignored; `_maybe_inject_sa_key` context manager handles the optional `creds.extras["gcp_service_account_json"]` escape hatch and restores the prior `GOOGLE_APPLICATION_CREDENTIALS` on exit. CLI `_apply_scope_flags` widened: `--scope bigquery:<project-id>` produces a descriptor whose `id` is the project id (no ARM URL) and leaves Azure coords unset; mixing Azure + GCP scopes in one `analyze-all` invocation is supported. 14 new tests in `tests/sources/bigquery/test_provider.py` (discover happy-path + None creds + ignored sub_id; validate happy-path + ADC-failure short-circuit + per-probe isolation + project-id pinning; make_clients bundle + SA-key path + missing-id rejection; SA-key context manager set/restore/remove/noop) + 2 new CLI scope-flag tests. **546 Python tests passing.** |
| 2026-05-20 | 5 | **Phase 5 pre-flight — design decisions locked** (commit `cc5f446`). (1) `SourceType.BIGQUERY = "bigquery"` added to the enum (matches the existing SPA `sourceTypes.ts` union). (2) `SourceProvider` Protocol widened: `discover`/`validate`/`make_clients` now type `creds` as `Credentials \| None`. `Credentials` docstring explicitly calls out that non-Azure providers (BigQuery via ADC) may ignore the object entirely; the optional `creds.extras['gcp_service_account_json']` escape hatch is documented. (3) `doctor._check_scopes`: AAD env-var gate moved from module-level early-return to per-scope decision. Synapse/ADF/Databricks scopes still skip when AAD env vars are missing; BigQuery (and future non-Azure) scopes proceed and receive `None` creds so the provider can resolve via ADC. (4) `pyproject.toml`: new `[bigquery]` optional extra pinning `google-cloud-bigquery>=3.25`, `google-cloud-resource-manager>=1.13`, `google-cloud-logging>=3.12`, `google-auth>=2.30`; `[dev]` rolled up to include both `[databricks]` + `[bigquery]`. (5) Manifest Phase 5 §Scope: auth-model block (ADC-only), env-var contract (`SMA_SOURCE_TYPE=bigquery`, `SMA_GCP_PROJECT_ID`, `GOOGLE_APPLICATION_CREDENTIALS`, `SMA_GCP_BILLING_ACCOUNT`), enum value, and first checklist item all locked. **530 Python tests passing.** |
| 2026-05-20 | 1.5 | **Phase 1.5 closed.** Audited the seven carry-over items: four shipped under downstream slice names (CLI `--scope` + `web/api/runs.py` scopes in `3b27727`, MODULE_SPECS partial via `run_plan.py`, getting-started Mermaid in `d4d67b3`), two were superseded by the Phase-2.5 / 4-D `Configuration.tsx` pattern (the Phase-0 `SourcePicker` + `ScopeList` components were never mounted; `apiDiscoverByType` replaced by per-source `apiDiscoverWorkspaces` / `apiDiscoverFactories` / `apiDiscoverDatabricksWorkspaces`), and one was the only genuine gap. New `doctor._check_scopes()` registered in `run_checks`: iterates `cfg.scopes`, looks up `get_provider(scope.type)`, calls `provider.validate()`, emits one `CheckResult` per scope with provider-aggregated detail. Skips gracefully when `--offline`, when AAD env vars are missing, when `load_config` raises, or when a provider stub returns `NotImplementedError`. 5 new tests in `test_doctor.py`. Phase-0 stub components `web/src/components/SourcePicker.tsx` + `ScopeList.tsx` deleted; `SourceTypeId` lifted to a new dedicated `sourceTypes.ts` module (used only by `EstateSummaryCard.tsx` + `Dashboard.tsx`). Manifest checklist ticked + annotated with shipped / superseded labels. **529 Python tests passing**, **25 vitest tests passing**, SPA build clean. |
| 2026-05-20 | 4 | **Estate Overview Databricks CU rollup** (commit `bc190b5`). Databricks-only scopes were showing blank `projected_fabric_cu` and no `recommended_fabric_sku` in the Estate Overview because `web/estate.py::_extract_run` only summed dedicated-pool `capacity_projection.estimated_cu` + pipelines `integration_daily_cu`. Added `_databricks_daily_cu(databricks)` helper that mirrors `_integration_daily_cu`: prefers the 7-day window per workflow / interactive cluster, sums `est_cu_hours_fabric_spark` across `workflow_run_stats` + `interactive_cluster_usage`, converts CU-hr/window to sustained CU/day. Folded into `projected_fabric_cu` alongside DW + pipelines; when `capacity.get("recommended_sku")` is falsy and `projected_fabric_cu > 0`, derives `recommended_fabric_sku` via lazy-imported `_smallest_sku_covering(projected_fabric_cu)` from `fabric_mapping.cu_projection`. **524 Python tests passing.** |
| 2026-05-20 | 4 | **Slice 4-F-2 backend resolution fix** (commit `7a26cd8`). Shallow `jobs.list()` was returning empty `tasks` / `job_clusters` for many real workspaces, and stripped `cluster_spec` from `list_runs()` payloads + ephemeral terminated clusters were producing `0.00 vCore-hr` headlines. Three fixes in `modules/databricks_workflows/collector.py`: (a) `iter_workflows_with_tasks` calls `jobs.list(expand_tasks=True)` with TypeError fallback, plus `_convert_job_with_fallback` retries via `jobs.get(job_id)` when tasks AND job_clusters are both empty; (b) `iter_workflow_runs` uses `expand_tasks=True` on `list_runs` and reads `cluster_spec` from `raw.tasks[]` when run-level is missing; (c) new gated `clusters.get(cluster_id)` post-mortem cache fills cluster shape for ephemeral terminated clusters \u2014 controlled by `SMA_DATABRICKS_CLUSTER_POSTMORTEM=1` (default on; `0/false/no/off` disables). `DatabricksWorkflowsAnalyzer` ctor + `for_descriptor()` plumbed the gate; caveats block emitted when disabled and unresolved runs exist. 7 new tests in `tests/test_databricks_vcore_hours.py`. **524 Python tests passing.** |
| 2026-05-20 | 4 | **Slice 4-F UI** (commits `3cb6cf3`, `b01d2bc`, `7da4e2a`, `562b890`, `ebc3737`). Dashboard surfaces Databricks workflow vCore-hours next to dedicated-pool DWU-hours: headline tile, 28-day daily-trend chart with average line, 24-hour hourly chart sibling, runs/day fallback when vCore-hr is 0, and interactive-cluster usage folded into the headline tile. Source-aware so Synapse / ADF dashboards unchanged. |\n| 2026-05-20 | 4 | **Slice 4-F backend** (commit `5d594bd`). `modules/databricks_workflows/collector.py` ships `iter_workflow_runs` over `jobs.list_runs`; new `run_stats.py` computes `est_cu_hours_fabric_spark` per workflow + interactive cluster using a 7d/28d windowing scheme (DBU \u2192 vCore-hr \u2192 Fabric Spark CU-hr conversion documented in module docstring). Analyzer collects and writes `workflow_run_stats` + `interactive_cluster_usage` arrays into `databricks_workflows.json`. Fix commits `52ab833`, `0feba9f`, `aa00be3` plumb `SMA_SOURCE_TYPE=databricks` through `load_config`, fix `_make_workspace_client` kwarg signature, and pre-fetch AAD tokens instead of using the SDK `credentials_provider` hook. |
| 2026-05-20 | 5 | **Phase 5 re-scoped: Google BigQuery replaces SAP BW.** Rationale: higher Fabric-migration demand + mature first-party Python SDK stack vs SAP BW's `pyrfc` native-build pain. SAP BW deferred to Phase 6+; Phase-0 stubs in `sources/sap_bw/` and the `[sapbw]` extra retained for later revival. Evaluated [`constantino-casado/GCP_logs`](https://github.com/constantino-casado/GCP_logs) as a candidate SDK and **declined**: GPL-3.0 (we are MIT \u2014 incompatible), packaged as a Tkinter desktop app (no public Python API), 4 days old / 0 stars / 0 releases, and scope is audit-log-only (`jobservice.jobcompleted`, hard-coded 30-day filter). Kept as a **reference implementation** for the Cloud Logging filter + audit-log field-flattening logic only; production code will depend directly on `google-cloud-bigquery`, `google-cloud-resource-manager`, `google-cloud-logging`, and `google-auth` under a new `[bigquery]` optional extra. New §Phase 5 scope checklist drafted (provider, `bigquery_workloads` module, fabric_mapping rules, SPA Configuration / Run page, `docs/user-guide/22-bigquery.md`, estate-overview `_bigquery_daily_cu`). |
| 2026-05-20 | 4 | Phase 4 Slice 4-E — docs + polish (commit `d4d67b3`). New `docs/user-guide/21-databricks.md` mirrors `20-data-factory.md`: auth (SP → Reader on the workspace + workspace `User` entitlement so `/api/2.1/jobs/list` works), Configuration / Run page tour, collected shapes (`iter_jobs_with_tasks`, `list_job_clusters`, `list_schedules`), explicit limits (no run history yet, no Unity Catalog, no DLT pipelines), and a 4.5/4.6 roadmap. `01-getting-started.md` Mermaid: "Pick source type" now lists Databricks, auto-discover bubble widened, per-source-role hint adds the Databricks workspace-User callout, and the Run-page module list adds `databricks_workflows`. `docs/user-guide/README.md` index links the new chapter. `README.md` supported-sources block flips Databricks to alpha and adds the `databricks_workflows` bullet; `fabric_mapping` rollback-hint copy mentions "Databricks workflow". `QUICKSTART.md` opening paragraph + sources sidebar mention Databricks; module table adds row 4b (`databricks_workflows`, surfaced via `analyze-all`). **Phase 4 closes at 490 Python tests + 25 SPA tests passing.** |
| 2026-05-20 | 4 | Phase 4 Slice 4-D — SPA Configuration + Run page widened for Databricks (commit `3a22c3b`). Backend: `web/schemas.py` widens both `source_type` Literals to include `"databricks"`; `web/config_io.py` accepts `SMA_SOURCE_TYPE=databricks`, ships `discover_databricks_workspaces()` (mirrors `discover_factories`, surfaces `workspace_url` via the existing `sql_endpoint` slot), and routes `validate_config_live` through `DatabricksProvider.validate` + `.discover` when source_type=databricks (Synapse SQL gauntlet skipped). New route `POST /api/config/discover-databricks-workspaces` registered in `web/api/config.py`. Frontend: `SourceType` union widened to `"synapse_workspace" | "adf" | "databricks"`; new `apiDiscoverDatabricksWorkspaces()`; `pages/Configuration.tsx` adds a third radio (Synapse / ADF / Databricks) and replaces the binary `=== "adf"` ternaries with `scopeNoun() / scopeLabel() / discoverVerbObject() / discoverFor()` helpers so the dropdown / discover button / hint copy all switch correctly for Databricks; `pages/Run.tsx` adds `databricks_workflows` to `ALL_MODULES` and `MODULES_BY_SOURCE.databricks = {databricks_workflows, cost, fabric_mapping}` plus a per-source guidance line. New `tests/test_databricks_slice_4d.py` (7 tests) covers `.env` round-trip, `discover_databricks_workspaces` happy-path + missing-credentials + provider-error paths, the `validate_config_live` Databricks branch (skips Serverless SQL / Spark Livy probes), and end-to-end via TestClient hitting the new route. **490 Python tests passing**, **25 SPA tests passing**. |
| 2026-05-20 | 4 | Phase 4 Slice 4-C — cost + fabric_mapping widened to DATABRICKS (commit `bba5cbc`). `MODULE_SPECS["cost"].supports` and `MODULE_SPECS["fabric_mapping"].supports` now include `DATABRICKS`. `cost/cost_client.py::_classify_resource_id` adds branches for `Microsoft.Databricks/workspaces` (→ `databricks_workspace`) and `Microsoft.DataFactory/factories` (→ `adf_factory`, retroactively filling a Phase-2 gap). `fabric_mapping/rules.py` ships `rules_for_databricks_workflows(payload, *, source_type=None)` emitting an inventory rec, per-task-type unsupported/partial/unknown buckets, a Delta Live Tables signal, a pre-13.x runtime warning, and an interactive-cluster sprawl info rec. `fabric_mapping/analyzer.py` registers `databricks_workflows.json` in `_INPUT_FILES` + the new rule in `_RULES`. `fabric_mapping/runbook.py::_AREA_TO_PHASE` routes `databricks_workflows`/`...tasks` → `orchestration_migration` and `...clusters` → `compute_migration`; `_rollback_hint_for` adds dedicated copy for both. `test_module_spec.py` + `test_run_plan.py` flipped: Databricks now has three applicable modules (`databricks_workflows`, `cost`, `fabric_mapping`) and `plan_run(["pipelines","cost"], dbx_scope)` runs `cost` while skipping `pipelines`. New `tests/test_databricks_slice_4c.py` (13 tests) covers supports matrix, ARM-id classifier, every rule branch (inventory / unsupported / partial / unknown / DLT / runtime drift / interactive sprawl / empty payload), analyzer wiring, and runbook phase + rollback hint mappings. **483 Python tests passing**. |
| 2026-05-19 | 0 | Feasibility study + scaffolding skeleton committed to `unified_SMA` |
| 2026-05-19 | 0 | Phase-0 scaffolding committed (`df53c7d`): sources/ + modules/spec.py + 20 passing tests + ADRs 0001–0004 + architecture docs + UI stubs |
| 2026-05-19 | 1 | Phase-1 foundational refactor: Synapse provider impl, `AppConfig.scopes`, `plan_run()`, manifest v2 with v1 auto-upgrade, `config_io` shim. 59 tests passing. Phase-1.5 follow-ups (CLI flag / SPA wiring / MODULE_SPECS callers) carved out. |
| 2026-05-19 | 0 | D1 signed off: **multi-scope per run** is the model. Manifest v2, `plan_run()`, and `ScopeRecord` are already built around it; user-facing CLI/SPA exposure of multi-scope lands in Phase 1.5+. |
| 2026-05-19 | 0 | D2 signed off: **rename to USMA in Phase 3** with one-release `DeprecationWarning` shim. Both `synapse-migration-analyzer` and `usma` CLI entry points coexist during the shim window. |
| 2026-05-19 | 0 | D3–D10 signed off: shared SP + per-source extras (D3); source-type tabs (D4); keep `pipelines` until Phase 4 (D5); optional extras for native deps (D6); manifest v2 location confirmed (D7); readable `short_scope_id` slug, not hash (D8); hardcoded `supports` on `ModuleSpec` (D9); React context introduced only when ≥3 consumers need it (D10). All ADRs flipped to Accepted. |
| 2026-05-19 | 2 | Phase-2 backend foundation: real `AdfProvider` (discover/validate/make_clients) + 6 unit tests; `PipelineCollector` Protocol + `SynapseCollector` / `AdfCollector` adapters + 8 unit tests; `MODULE_SPECS.supports` widened for `pipelines`/`cost`/`fabric_mapping`; `azure-mgmt-datafactory` dep added. 73 tests passing. Analyzer dispatch wiring + ADF fixtures deferred to follow-up. |
| 2026-05-19 | 3 | Phase-3 prep: `usma` CLI entry point registered alongside `sma`; optional source extras `[databricks]`, `[sapbw]`, `[ssis]` added per D6. Full UI polish + package directory rename deferred to subsequent PRs. |
| 2026-05-19 | 2 | Phase-2 analyzer dispatch + ADF fixture: `PipelinesAnalyzer` accepts a `PipelineCollector` via constructor (backward-compat default still wraps `ArtifactsApiClient` as `SynapseCollector`); `PipelinesAnalyzer.for_descriptor()` factory routes by `descriptor.type`; Synapse-only run-history step gated. `tests/fixtures/sources/adf/factory.json` + 2 integration tests exercise the ADF path end-to-end without Azure. **404 tests passing** across full suite. |
| 2026-05-19 | 1.5 | Phase-1.5 backend wiring (commit `3b27727`): new `ScopeRef` Pydantic model on `web.schemas`; `RunMeta.scopes` persisted on every run start; `StartRunRequest.scopes` optional input; `web/api/runs.py` forwards body.scopes into `JobRunner.start(scopes=...)`; CLI gains repeatable `--scope <type>:<name>[@<sub>/<rg>]` flag on `analyze-all` with a `_apply_scope_flags` parser. 6 new parser unit tests. **410 tests passing**. |
| 2026-05-19 | 2 | Phase-2 docs (commit `6d54b93`): new `docs/user-guide/20-data-factory.md` documenting the ADF alpha, support matrix, `--scope` flag, and Phase 2.5 roadmap. |
| 2026-05-19 | 2/3 | Phase-2 + Phase-3 SPA delta (commit `c68a3e2`): `ScopeRef` TS type on the loader; `RunMeta.scopes` optional on the SPA model; `apiStartRun` accepts an optional `scopes` arg; RunsHistory gains a **Scopes** column showing count + per-source-type pills; Dashboard mounts the `EstateSummaryCard` whenever the current run has multi-scope context. 16/16 vitest tests still green. |
| 2026-05-19 | 2.5 | Phase-2.5 SPA wiring (commit `7337c06`): `SMA_SOURCE_TYPE` env plumbed end-to-end. `AzureConfigPublic.source_type` exposed in the API; `web/config_io` reads/writes it via `_SOURCE_TYPE_KEY`; `POST /api/config/discover-factories` mirrors the workspace shape for ADF. Configuration page swaps the workspace dropdown for a factory dropdown based on the radio toggle. `config.load_config` builds the right scope (Synapse workspace ARM ID or ADF factory ARM ID); `modules/__init__._pipelines` dispatches via `PipelinesAnalyzer.for_descriptor`. |
| 2026-05-19 | 2.5 | Phase-2.5 validation fix (commit `b1beb84`): `validate_config_live` ADF branch was returning the dataclass `sources.ConfigCheck`, but `/api/config/validate` is typed against the Pydantic `web.schemas.ConfigCheck`. Added an explicit conversion loop so the API stops returning 500s when validating an ADF config. |
| 2026-05-19 | 2.5 | Phase-2.5 output-quality fixes (commit `f0fa574`): `AdfCollector.endpoint` returns the factory ARM ID so `artifacts_endpoint` is correct for ADF. New `Trigger.type_properties` field plus collector wiring carries the raw recurrence payload through `model_dump()` so the schedule mapper resolves cadence for ADF triggers (was rendering `Recurring every None ?`). |
| 2026-05-19 | 2.5 | Run-page module gating (commit `8867902`): new `MODULES_BY_SOURCE` allow-list filters the analyzer checkboxes by `source_type`. ADF mode shows only `pipelines`; the Synapse-only analyzers are hidden, pruned from any pre-existing selection, and re-filtered at submit time so stale state can't smuggle them through. |
| 2026-05-19 | 2.6 | ADF run-history (commit `f45451f`): new `AdfRunHistoryClient` mirrors the Synapse `RunHistoryClient` over `DataFactoryManagementClient.pipeline_runs.query_by_factory` / `activity_runs.query_by_pipeline_run`. Analyzer dispatch picks the right client by `isinstance(collector, AdfCollector)`; `_collect_run_history` stays source-agnostic. The 'pipeline runs' panels now render real data, daily / hourly status, and aggregated success rates for ADF factories. **421 Python tests passing**. |
| 2026-05-19 | 3 | Phase-3 — Dashboard scope-aware pipelines (commit `76e9810`): final Phase-3 UI slice. Pipelines is the cross-source module — both Synapse and ADF emit `pipelines.json` — so the Dashboard now loads it per-scope via `loadPipelinesPerScope` (wrapper over `loadModulePerScope<PipelinesReport>('pipelines')`), shows a `ScopeFilter` pill row above the pipelines area, and renders one `PipelinesSection` per visible scope. The section picks up an optional `scope` prop and surfaces `<source_type> · <slug>` as a muted pill in its `<h2>` when in multi-scope mode (heading byte-identical otherwise). `integrationDailyCu` (steady-state CU feeding the SKU stat card) is a portfolio number, so it sums across *every* scope regardless of the pill — only rendered sections are filter-scoped. Same pass also fixes a same-second timestamp flake on the new multi-scope diff tests (1.1s sleep between the two POSTs so run-ids are guaranteed to differ). **441 Python, 25 vitest, `tsc --noEmit` clean.** Phase 3 UI is now complete. |
| 2026-05-19 | 3 | Phase-3 — multi-scope diff (commit `172a16e`): closes the Diff verification box. Backend `web/api/diff.py` swaps the old root-only `glob('*.json')` for `_collect_artifacts()` that walks root + one level of `<source_type>__<slug>/` sub-directories; root-level artefacts keep their bare `name` (legacy diffs stay byte-identical) while per-scope artefacts get scope-prefixed names (`synapse_workspace__foo/fabric_mapping`) so `diff_manifests` pairs same-scope artefacts across runs and never collapses two scopes onto a colliding key. SPA `RunDiff.tsx` splits the prefixed name into a separate Scope column (`pill muted`, `—` for legacy flat runs). Two new tests in `tests/web/test_scope_artefacts.py` pin the multi-scope diff shape *and* the legacy flat-name shape. **441 Python (+2), 25 vitest, `tsc --noEmit` clean.** |
| 2026-05-19 | 3 | Phase-3 UI — Runbook scope-aware (commit `1689928`): same pattern as Recommendations, two pages now share the `ScopeFilter` + `useScopeFilter` + `loadModulePerScope` idiom. `Runbook.tsx` swaps `loadFabricMapping()` for a bundle loader that merges every scope's `runbook[]` into one tagged stream (`_scopeDir`, `_scopeLabel` on each step). Effort-summary card + workspace name on the title follow the picked scope; "All" falls back to the primary report so single-scope runs stay byte-identical. The per-phase tables grow an extra Scope column only when scopes.length > 1 and selection is `all`; the Markdown export grows a matching column. The localStorage step-status key follows the active scope's workspace, so each scope tracks its own ticks. **439 Python, 25 vitest, `tsc --noEmit` clean.** |
| 2026-05-19 | 3 | Phase-3 UI — scope filter pills (commit `a015af8`): first end-to-end consumer of the 2.7 plumbing. New `web/src/components/ScopeFilter.tsx` with `useScopeFilter(runId, scopes)` hook + `matchesScope` helper, persisted in `sessionStorage` keyed per run id, auto-falls-back to `{kind:'all'}` on single-scope runs. Wired into `Recommendations.tsx` via `loadModulePerScope('fabric_mapping')` — every recommendation is now tagged with its `_scopeDir`/`_scopeLabel` and the toolbar grows a pill row with live per-scope counts that respect the active severity/area/impact/search filter. RecRow surfaces an inline scope chip so the 'All' view stays legible. Pure backward compat for single-scope runs (component renders null, `_scopeDir` is null, existing rollup/grouping/etc. all unchanged). `docs/user-guide/01-getting-started.md` Mermaid refreshed: source-type branch (Synapse vs ADF), multi-scope fan-out box, per-scope artefacts on completion, scope filter pill node, scope count + source chips on Runs history. 4 new vitest cases pinning `matchesScope` semantics. **439 Python, 25 vitest, `tsc --noEmit` clean.** |
| 2026-05-19 | 2.7 | Multi-scope dispatch — SPA Commit C (commit `b674186`): scope-aware loader. `web/src/api/loader.ts` gains `apiListScopes(runId)` (cached `GET /api/runs/{id}/scopes`) and `loadModulePerScope<T>(module)` returning `ScopedPayload<T>[] = { scope: RunScope | null, payload: T | null }[]`. Three-mode dispatch: static / no-run-id and control-plane single-scope return one un-scoped entry (legacy shape); multi-scope returns one entry per scope with the right `?scope=` URL. Modules absent from `scope.modules` short-circuit to `payload: null` with no network call. Existing single-payload loaders (`loadDedicatedPools`, `loadPipelines`, etc.) untouched — Phase 3 UI pages opt in. 5 new vitest cases in `web/src/__tests__/loader-scopes.test.ts`; **21 vitest tests passing**, `tsc --noEmit` clean. Phase 2.7 is now complete; Phase 3 UI can render per-scope panels with no additional plumbing. |
| 2026-05-19 | 2.7 | Multi-scope dispatch — backend Commit B (commit `762e3ca`): scope-aware artefact serving in the API. `FilesystemRunRepo.scope_module_path(run_id, scope_dir, module)` and `list_scopes(run_id)` enforce the `<source_type>__<slug>` sub-directory contract with the same path-traversal defence as `module_path`. `GET /api/runs/{id}/modules/{module}` accepts `?scope=<dir>` (omitted → legacy flat lookup); new `GET /api/runs/{id}/scopes` route enumerates per-scope subdirs with their module artefacts. 11 new tests in `tests/web/test_scope_artefacts.py` cover validation, enumeration, route round-trips, 404/400 error shapes, and the legacy single-scope path. **439 Python tests passing**. Next: 2.7-C SPA loader scope-walking. |
| 2026-05-20 | 3 | Source-platform alignment — Slice A backend (commit `b0e52d4`): added `source_type: str | None` field to `Recommendation` and `RunbookStep` Pydantic models (defaults to `None` for backward compat on legacy artefacts). `rules_for_pipelines(payload, *, source_type=...)` and `_rollback_hint_for(rec)` now branch on `source_type == "adf"` to emit "Azure Data Factory pipeline(s) to migrate" / "… migrate to Fabric Data Factory (Data pipelines)" wording instead of Synapse phrasing. Verification runbook step is now platform-neutral ("source scope (workspace / factory)"). `FabricMappingAnalyzer.run()` stamps the primary scope's source type onto every recommendation and runbook step it emits. 6 new tests in `tests/test_source_type_phrasing.py` pin default/synapse/adf wording. **447 Python tests passing**. |
| 2026-05-20 | 3 | Source-platform alignment — Slice B Dashboard (commit `281ee99`): new `SOURCE_TYPE_LABELS` + `SOURCE_NOUN_LABELS` dicts in `web/src/lib/labels.ts` with `sourceLabel(raw)` / `sourceNoun(raw)` helpers (fallback `"Scope"` / `"scope"` for legacy runs). Dashboard title falls back to `({sourceNoun})` so ADF runs say `(factory)` instead of `(workspace)`; new source-type pill chip surfaces next to the generated-at timestamp; `PipelinesSection` scope chip switched from raw `{scope.source_type}` to `sourceLabel(...)`; pipeline reanalyze tooltip now reads "Re-run the pipelines analyzer for this factory/workspace" per source. `tsc --noEmit` clean, **25 vitest tests passing**. |
| 2026-05-20 | 3 | Source-platform alignment — Slice C Recommendations + Runbook (commit `b28fe62`): TS `Recommendation.source_type` + `RunbookStep.source_type` added to match Slice A. Per-row `_scopeLabel` on both pages now renders `sourceLabel(scope.source_type) · slug` (e.g. "Azure Data Factory · main-factory") instead of leaking the raw `adf` / `synapse_workspace` enum tokens. **25 vitest tests passing**, `tsc --noEmit` clean. |
| 2026-05-20 | 3 | Source-platform alignment — Slice D EstateOverview (commit `b899b83`): backend `EstateWorkspace.source_type` derived from `run.json`'s `scopes[0].source_type`; `_extract_run()` plucks it from the scopes list; `/api/estate/export.csv` gained a `source_type` column. UI: "Cross-scope, cross-time rollup" wording, new **Source** column with platform chip in the workspaces table, "Scopes" stat card + section heading, "other scopes" copy in the control-plane-only banner, top-blockers "Scopes" header. **447 Python + 25 vitest tests passing**, `tsc --noEmit` clean. Phase 3 UI exit criterion now met. |
| 2026-05-20 | 3 | Help UI Mermaid rendering + multi-source doc sweep (commit `d4fcf6a`): `web/src/components/Markdown.tsx` gains a `MermaidBlock` that lazy-imports `mermaid@^11.15.0` and intercepts ` ```mermaid ``` ` code fences; `securityLevel: "strict"` prevents arbitrary HTML in the source from escaping the rendered SVG; falls back to a flagged `<pre>` on parse error. New `.markdown-body .mermaid` styling in `styles.css`. The Process-overview diagram in `docs/user-guide/01-getting-started.md` now renders in `/help/01-getting-started` instead of showing as plain text. Plus a README.md + QUICKSTART.md sweep: multi-source framing, USMA rename callout, supported-source matrix, per-module Synapse-only-vs-cross-source annotations, Configuration source-type radio + multi-scope picker, EstateOverview Source chip + CSV `source_type` documented. **25 vitest passing**, `tsc --noEmit` clean. |
| 2026-05-20 | 3 | Estate Overview — Source-mix stat card (commit `704c308`): new top-grid stat card aggregates `EstateWorkspace.source_type` across all scopes (e.g. `1 Synapse workspace · 1 Azure Data Factory`), with sub-text "N source types in this estate" or "all scopes share one source type" for uniform estates. Complements the per-row Source column landed in Slice D. **25 vitest passing**, `tsc --noEmit` clean. |
| 2026-05-20 | 3 | Estate Source chip — forward + backward fix (commit `ad740a1`): legacy single-scope runs were rendering "—" in the Source column because `RunMeta.scopes` was empty (the legacy Run page never passed `scopes` to `JobRunner.start`). **Forward fix:** `web/jobs.py:start()` now synthesizes a `ScopeRef` from `cfg.scopes[0]` when the caller omits the arg, so all new runs persist a real `scopes[0].source_type`. **Backfill:** `web/estate.py:_extract_run()` falls back to the first `fabric_mapping.json` recommendation's `source_type` (stamped since Slice A) when `run.json.scopes[]` is empty, lighting up existing legacy runs without re-running. Defensive `getattr(cfg, "scopes", None)` keeps the `SimpleNamespace` test cfg shape green. **447 Python tests passing**. |
| 2026-05-20 | 4 | Phase 4 Slice 4-B — `databricks_workflows` module (commit `5cff105`): new sibling of `pipelines` per ADR D5. `modules/databricks_workflows/` ships `models.py` (Workflow / WorkflowTask / JobCluster / InteractiveCluster / DatabricksWorkflowsAnalysis), `collector.py` (`DatabricksWorkflowsCollector` walks `WorkspaceClient.jobs.list()` + `clusters.list()`, classifies task types + cluster references with duck-typed access so dict / SimpleNamespace / SDK enum payloads all work), `analyzer.py` (`DatabricksWorkflowsAnalyzer.for_descriptor()` builds a `WorkspaceClient` via `DatabricksProvider`), `reporting.py` (JSON / CSV / Markdown), and `fabric_compat.py` (coarse task-type → support label mapping — widened in Slice 4-C). Registered in `MODULE_REGISTRY` (between `fabric_validation` and `fabric_mapping`) + `MODULE_SPECS` with `supports={DATABRICKS}`. `test_run_plan.py` + `test_module_spec.py` updated to reflect that `databricks_workflows` is Databricks-only and that Synapse-scope runs now have one less applicable module. 11 new tests cover classification, collector walking, analyzer aggregation + descriptor validation, reporting, and registry wiring. **470 Python tests passing**. |
| 2026-05-20 | 4 | Phase 4 Slice 4-A — `DatabricksProvider` (commit `4e65dbc`): replaced the Phase-0 stub with a real provider. `discover()` calls `azure.mgmt.databricks.AzureDatabricksManagementClient.workspaces.list_by_subscription()` and stamps `workspace_url` / `workspace_id` / `sku` / `managed_resource_group_id` into descriptor `extras`. `validate()` does an ARM Reader probe (`workspaces.get(rg, name)`) then a data-plane probe via `databricks.sdk.WorkspaceClient.current_user.me()` — AAD federation by default (`credentials_provider=lambda` returning `{"Authorization": f"Bearer {tok.token}"}` with `cred.get_token("2ff814a6-3304-4ab8-85cb-cd0e6f879c1d/.default")`), PAT path when `DATABRICKS_TOKEN` is present in `creds.extras`. `make_clients()` returns a `DatabricksClientBundle` dataclass. `[databricks]` extra now pins `azure-mgmt-databricks>=2.0`. 12 new tests in `tests/sources/databricks/test_provider.py` (discover requires sub_id, ARM probe path, data-plane probe path, PAT path, ARM-failure short-circuit, missing workspace_url, bundle shape, descriptor validation, ARM-id parsing). Phase-0 `test_unimplemented_stubs_raise_on_discover` updated to drop DATABRICKS. **459 Python tests passing**. |
| 2026-05-19 | 2.7 | Multi-scope dispatch — backend Commit A (commit `2bceef6`): `JobRunner._run` per-scope inner loop. When `len(meta.scopes) > 1`, each selected module executes once per scope into `run_dir/<source_type>__<slug>/` with `azure.workspace_name` / `azure.resource_group` swapped via `dataclasses.replace` and `SMA_SOURCE_TYPE` env stamped for the duration. Single-scope runs keep the legacy flat layout — zero regression for the 421-test baseline. Helpers `_scope_slug`, `_cfg_for_scope`, `_stamp_source_type_env`, `_is_multi_scope` live in `web/jobs.py`. 7 new tests in `tests/web/test_multi_scope_dispatch.py`. **428 Python tests passing**. Follow-ups 2.7-B (backend route + manifest writing) and 2.7-C (SPA loader scope-walking) unlock the user-visible Phase 3 UI work. |


---

## Phase 5.3 — Cross-platform / packaging

**Status: 🟢 complete — 2026-05-31 (shipped as 5.3.0).**

Goal: Make USMA run natively on Windows, Linux (Ubuntu Desktop tested), and macOS without per-host code paths leaking into module logic.

### What landed

- **New helper module** [src/usma/platform.py](src/usma/platform.py):
  - `cache_dir()` / `config_dir()` / `data_dir()` — XDG Base Directory spec on Linux/macOS (honour `$XDG_CACHE_HOME`, `$XDG_CONFIG_HOME`, `$XDG_DATA_HOME`), `%LOCALAPPDATA%` / `%APPDATA%` on Windows.
  - `default_odbc_driver()` — prefers `ODBC Driver 18 for SQL Server`, auto-falls back to driver 17 when only 17 is installed (covers older Ubuntu LTS).
  - `odbc_install_hint()` — apt snippet on Linux, brew snippet on macOS, MSI link on Windows; surfaced by `sma doctor`.
  - `default_runs_dir()` — `$SMA_RUNS_DIR` → `./runs` (if present) → OS per-user data dir.
- **[quickstart.sh](quickstart.sh)** — Bash bootstrapper mirroring `quickstart.ps1` for Linux/macOS (clone, `python3.12 -m venv .venv`, install all extras, build SPA, run `sma doctor --offline`, launch `sma serve --with-api --static-dir web/dist`). Flags: `--skip-clone`, `--branch`, `--python`, `--skip-doctor`, `--skip-web-build`, `--no-serve`.
- **[.gitattributes](.gitattributes)** — keeps `*.sh` LF on checkout so bash never sees CRLF.
- **[QUICKSTART.md](QUICKSTART.md)** §0.1 ships ODBC install commands for Ubuntu/Debian (apt) and macOS (brew); §0.2 adds a "Fast path — `quickstart.sh` (Linux / macOS)" subsection.
- **Wiring**: [src/usma/config.py](src/usma/config.py) defaults `SQL_ODBC_DRIVER` via `default_odbc_driver()`; [src/usma/doctor.py](src/usma/doctor.py) emits per-OS install hints; [src/usma/modules/cost/fabric_pricing.py](src/usma/modules/cost/fabric_pricing.py) cache path uses `platform.cache_dir()`; [src/usma/cli.py](src/usma/cli.py) `serve --with-api --runs-dir` default uses `default_runs_dir()`.
- **Tests**: 14 new tests in [tests/test_platform.py](tests/test_platform.py) cover per-OS dir resolution, XDG overrides, runs-dir fallback chain, ODBC 18→17 fallback, and per-OS install-hint phrasing.

### Deferred

- Apple-native `~/Library/Caches` / `~/Library/Application Support` paths on macOS (currently uses XDG layout — easier dotfile sharing between Linux and macOS dev hosts).
- CI matrix entry for `ubuntu-latest` and `macos-latest`.

---

## Recent housekeeping & bug-fix log (5.2.x – 5.3.x)

Cumulative ship log for changes that aren't substantial enough for their own phase row.

| Version | Commit | Change |
|---|---|---|
| 5.2.0 | `dd14f6d` | Help manifest: added chapters 16, 20–23, 21a, 99 + Sources section; fixed EstateOverview help link |
| — | `fe01d14` | Brand rename: "Unified Synapse" → "Unified Solution Migration Analyzer" (USMA acronym preserved) |
| — | `a7d63b7` | Estate Overview: removed "readiness over time" card |
| — | `459a6d5` | Dashboard: Snowflake daily chart → 7 days; new SnowflakeHourlyBars (last 24h, mirrors BigQuery) |
| 5.2.1 | `5af389c` / `00fc3fc` | Ruff lint clean-up (unused imports, stray f-prefixes, bad noqa) |
| 5.3.0 | `1859812` + `ed9522b` | Cross-platform support (see Phase 5.3 above) |
| 5.3.1 | `e99ab27` | Fix: `DELETE /api/runs/{id}/data` now stamps stale `running`/`queued` runs (no live worker) as `failed` and deletes them instead of 409-ing forever. Regression test in [tests/web/test_delete_stale_run.py](tests/web/test_delete_stale_run.py). |
| 5.3.2 | `af100ce` | Fix: RunsHistory SPA Delete button enabled for stalled `running` runs (was disabled, making the 5.3.1 backend fix unreachable). Tooltip updated; SPA rebuilt. |

