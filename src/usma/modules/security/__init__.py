"""Security module.

Mid-term roadmap scaffolding (v0): captures workspace and dedicated-pool firewall
rules, AAD-only enforcement state, TLS minimum version, encryption-at-rest config,
and a per-pool secrets/credentials inventory (linked-service credential *types*
only — values are never read or written anywhere).
"""
